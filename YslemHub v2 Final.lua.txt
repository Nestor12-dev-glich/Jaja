-- ===================================================================
-- YSLEM HUB v2 — Steal a Brainrot
-- UI redesign + Auto Carry on Grab + fixes
-- ===================================================================
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local LP = Players.LocalPlayer

-- Discretion : noms aleatoires + parent cache (1 table = economie de locals)
local DISC = {}
function DISC.name()
	local chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
	local s = ""
	for _ = 1, math.random(8, 14) do
		local i = math.random(1, #chars)
		s = s .. chars:sub(i, i)
	end
	return s
end
function DISC.hide(inst)
	local ok = pcall(function()
		if gethui then inst.Parent = gethui()
		elseif syn and syn.protect_gui then syn.protect_gui(inst); inst.Parent = game:GetService("CoreGui")
		else inst.Parent = game:GetService("CoreGui") end
	end)
	if not ok or not inst.Parent then
		pcall(function() inst.Parent = LP:WaitForChild("PlayerGui") end)
	end
end

-- FORWARD DECLARATIONS (fix crash mobile)
local startAutoLeft, stopAutoLeft, startAutoRight, stopAutoRight
local startAntiRagdoll, stopAntiRagdoll
local setupMedusaCounter, stopMedusaCounter
local BC
local applyFPSBoost, autoSaveConfig
local startAutoSteal, stopAutoSteal
local refreshUIToggles
local UIR = {}

local State = {
	normalSpeed=59, carrySpeed=28, laggerSpeed=13, laggerCarrySpeed=13,
	speedType="normal",
	laggerActive=false, laggerCarryActive=false,
	autoBatToggled=false, hittingCooldown=false,
	infJumpEnabled=false, infJumpMode="manual",
	antiRagdollEnabled=false, fpsBoostEnabled=false,
	guiVisible=true,
	isStealing=false,
	autoCarryOnGrab=true,
	medusaLastUsed=0, medusaDebounce=false, medusaCounterEnabled=false, medusaAutoReset=true,
	dropBrainrotActive=false,
	autoTpDownEnabled=false, autoTpDownY=15,
	autoLeftEnabled=false, autoRightEnabled=false,
	autoLeftPhase=1, autoRightPhase=1,
	_tpInProgress=false,
	lastMoveDir=Vector3.new(0,0,0),
	unwalkEnabled=false,
	keyAutoLeft=Enum.KeyCode.Unknown,
	keyAutoRight=Enum.KeyCode.Unknown,
	keyDropBR=Enum.KeyCode.Unknown,
	keyTpDown=Enum.KeyCode.Unknown,
	keyAutoBat=Enum.KeyCode.Unknown,
	keyCarrySpeed=Enum.KeyCode.Unknown,
	keyTpUp=Enum.KeyCode.Unknown,
	keyLaggerMode=Enum.KeyCode.Unknown,
	keyInstaReset=Enum.KeyCode.Unknown,
	keyLaggerUse=Enum.KeyCode.Unknown,
	_carryManualUntil=0,
	laggerUseEnabled=false, laggerUseAmount=45,
}

-- ===================================================================
-- AUTO STEAL
-- ===================================================================
local AutoSteal = {
	Enabled=false, Radius=60, Duration=1.3, IsStealing=false,
	Data={}, ProgressFill=nil, ProgressText=nil, RetryMax=2,
}

local _plotIsMyCache = {}

local function isMyPlotByName(plotName)
	local now = tick()
	local cached = _plotIsMyCache[plotName]
	if cached and (now - cached.t) < 2 then return cached.val end
	local plots = workspace:FindFirstChild("Plots")
	if not plots then _plotIsMyCache[plotName]={val=false,t=now}; return false end
	local plot = plots:FindFirstChild(plotName)
	if not plot then _plotIsMyCache[plotName]={val=false,t=now}; return false end
	local sign = plot:FindFirstChild("PlotSign")
	local r = false
	if sign then
		local yb = sign:FindFirstChild("YourBase")
		if yb and yb:IsA("BillboardGui") then r = yb.Enabled == true end
	end
	_plotIsMyCache[plotName] = {val=r, t=now}
	return r
end

local function getPlotOf(inst)
	local plots = workspace:FindFirstChild("Plots"); if not plots then return nil end
	local p = inst
	while p and p.Parent do
		if p.Parent == plots then return p end
		p = p.Parent
	end
	return nil
end

local _promptCache = nil
local _promptCacheTime = 0
local _lastHrpPos = Vector3.new(0,0,0)

local function findNearestPrompts()
	local now = tick()
	if _promptCache and (now - _promptCacheTime) < 0.08 then return _promptCache end
	local char = LP.Character
	local root = char and char:FindFirstChild("HumanoidRootPart")
	if not root then _promptCache={}; _promptCacheTime=now; return {} end
	local plots = workspace:FindFirstChild("Plots")
	if not plots then _promptCache={}; _promptCacheTime=now; return {} end
	local results = {}
	for _, plot in ipairs(plots:GetChildren()) do
		if not isMyPlotByName(plot.Name) then
			local podiums = plot:FindFirstChild("AnimalPodiums")
			if podiums then
				for _, pod in ipairs(podiums:GetChildren()) do
					pcall(function()
						local base = pod:FindFirstChild("Base")
						local spawn = base and base:FindFirstChild("Spawn")
						if spawn then
							local dist = (spawn.Position - root.Position).Magnitude
							if dist <= AutoSteal.Radius then
								local att = spawn:FindFirstChild("PromptAttachment")
								if att then
									for _, child in ipairs(att:GetChildren()) do
										if child:IsA("ProximityPrompt") then
											table.insert(results, {prompt=child, dist=dist, name=pod.Name})
											break
										end
									end
								end
							end
						end
					end)
				end
			end
		end
	end
	table.sort(results, function(a,b) return a.dist < b.dist end)
	_promptCache = results
	_promptCacheTime = now
	return results
end

RunService.Heartbeat:Connect(function()
	local char = LP.Character
	local hrp = char and char:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	if (hrp.Position - _lastHrpPos).Magnitude > 1 then
		_lastHrpPos = hrp.Position
		_promptCache = nil
	end
end)

local function initStealData(prompt)
	if AutoSteal.Data[prompt] then return end
	AutoSteal.Data[prompt] = {hold={}, trigger={}, ready=true, fails=0, useFallback=false}
	pcall(function()
		if getconnections then
			for _, c in ipairs(getconnections(prompt.PromptButtonHoldBegan)) do
				if c.Function then table.insert(AutoSteal.Data[prompt].hold, c.Function) end
			end
			for _, c in ipairs(getconnections(prompt.Triggered)) do
				if c.Function then table.insert(AutoSteal.Data[prompt].trigger, c.Function) end
			end
			if #AutoSteal.Data[prompt].hold == 0 and #AutoSteal.Data[prompt].trigger == 0 then
				AutoSteal.Data[prompt].useFallback = true
			end
		else
			AutoSteal.Data[prompt].useFallback = true
		end
	end)
end

local function tryStealOnce(prompt, data)
	local DUR = AutoSteal.Duration
	-- PAS de modification des propriétés du prompt (MaxActivationDistance etc.)
	-- Le serveur détecte ces changements → ban

	-- Démarre le hold visuel (si dispo) pendant la durée
	if not data.useFallback and #data.hold > 0 then
		pcall(function() for _, f in ipairs(data.hold) do task.spawn(f) end end)
	end
	-- Délai humanisé (légère variation pour éviter les patterns détectables)
	task.wait(DUR + math.random() * 0.08)
	-- Méthode 1 : fireproximityprompt
	if fireproximityprompt then
		local ok = pcall(function() fireproximityprompt(prompt) end)
		if ok then task.wait(0.05 + math.random() * 0.05); return true end
	end
	-- Méthode 2 : Triggered direct via getconnections
	if not data.useFallback and #data.trigger > 0 then
		local ok = pcall(function()
			for _, f in ipairs(data.trigger) do task.spawn(f) end
		end)
		if ok then task.wait(0.05); return true end
	end
	-- Méthode 3 : InputHold natif (le plus naturel, préféré)
	local ok = pcall(function()
		prompt:InputHoldBegin(); task.wait(DUR); prompt:InputHoldEnd()
	end)
	return ok
end

-- Carry mode automatique : activé UNIQUEMENT quand un Tool (brainrot) est dans les mains
local function setCarryMode(on)
	if not State.autoCarryOnGrab then return end
	if State.laggerActive or State.laggerCarryActive then return end
	local want = on and "carry" or "normal"
	if State.speedType == want then return end
	State.speedType = want
	if refreshUIToggles then refreshUIToggles() end
	if MobileButtons and MobileButtons.Buttons and MobileButtons.Buttons.carrySpeed then
		MobileButtons.Buttons.carrySpeed(on)
	end
	autoSaveConfig()
end

-- DÉTECTION RÉELLE du portage : le jeu force WalkSpeed ≈ 8 quand tu portes un brainrot
-- Puisqu'on utilise le proxy et qu'on ne touche plus WalkSpeed,
local _wsConn = nil

local function setupCarryDetection(hum) end  -- remplacé par setupFullDebug

local function setupFullDebug(char, humanoid, hrpPart)
	do return end  -- debug desactive

	-- 1) HRP children : brainrot soudé via Weld sur HRP
	if hrpPart then
		hrpPart.ChildAdded:Connect(function(c)
			print("[DEBUG HRP.ChildAdded]", c.ClassName, "Name=", c.Name)
		end)
		hrpPart.ChildRemoved:Connect(function(c)
			print("[DEBUG HRP.ChildRemoved]", c.ClassName, "Name=", c.Name)
		end)
	end

	-- 2) Attributs du character
	char.AttributeChanged:Connect(function(attr)
		print("[DEBUG CHAR.Attr]", attr, "=", tostring(char:GetAttribute(attr)))
	end)

	-- 3) Attributs + enfants du Player (objet, pas character)
	LP.AttributeChanged:Connect(function(attr)
		print("[DEBUG LP.Attr]", attr, "=", tostring(LP:GetAttribute(attr)))
	end)
	LP.ChildAdded:Connect(function(c)
		print("[DEBUG LP.ChildAdded]", c.ClassName, "Name=", c.Name)
	end)

	-- 4) WalkSpeed au cas où
	if humanoid then
		humanoid:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
			print("[DEBUG WalkSpeed] =", humanoid.WalkSpeed)
		end)
		-- Aussi JumpPower / autres props
		humanoid:GetPropertyChangedSignal("JumpPower"):Connect(function()
			print("[DEBUG JumpPower] =", humanoid.JumpPower)
		end)
	end

	print("[DEBUG] Actif - vole un brainrot et regarde F9")
end

local _toolWatchConns = {}
local function watchCharacterTools(char) end


local function executeSteal(prompt, animalName)
	if AutoSteal.IsStealing then return end
	if not prompt or not prompt.Parent then return end
	initStealData(prompt)
	local data = AutoSteal.Data[prompt]
	if not data or not data.ready then return end
	data.ready = false
	AutoSteal.IsStealing = true
	State.isStealing = true
	local startTime = tick()

	local progConn
	progConn = RunService.Heartbeat:Connect(function()
		if not AutoSteal.IsStealing then progConn:Disconnect(); return end
		local prog = math.clamp((tick() - startTime) / AutoSteal.Duration, 0, 1)
		if AutoSteal.ProgressFill then AutoSteal.ProgressFill.Size = UDim2.new(prog,0,1,0) end
		if AutoSteal.ProgressText then AutoSteal.ProgressText.Text = math.floor(prog*100).."%" end
	end)

	task.spawn(function()
		-- PAS de prompt.MaxActivationDistance = 9999 (détecté par le serveur)
		local success = false
		local attempts = 0
		while not success and attempts < AutoSteal.RetryMax do
			attempts = attempts + 1
			success = tryStealOnce(prompt, data)
			if not success then
				data.fails = (data.fails or 0) + 1
				if data.fails >= 3 then data.useFallback = true end
				task.wait(0.03)
			end
		end
		AutoSteal.IsStealing = false
		State.isStealing = false
		data.ready = true
		-- Note : auto-carry est déclenché par la détection de Tool ajouté au character
		-- (plus fiable que de se baser sur success qui ne dit pas si on a vraiment reçu l'animal)
		task.wait(0.4)
		if not AutoSteal.IsStealing and AutoSteal.ProgressFill then
			TweenService:Create(AutoSteal.ProgressFill, TweenInfo.new(0.3), {Size=UDim2.new(0,0,1,0)}):Play()
		end
		if AutoSteal.ProgressText then AutoSteal.ProgressText.Text = "0%" end
		_promptCache = nil
	end)
end

local autoStealConnection = nil
startAutoSteal = function()
	if autoStealConnection then return end
	local _t = 0
	autoStealConnection = RunService.Heartbeat:Connect(function()
		if not AutoSteal.Enabled or AutoSteal.IsStealing then return end
		local now = tick()
		if now - _t < 0.05 then return end
		_t = now
		local prompts = findNearestPrompts()
		if prompts and #prompts > 0 then
			executeSteal(prompts[1].prompt, prompts[1].name)
		end
	end)
end
stopAutoSteal = function()
	if autoStealConnection then autoStealConnection:Disconnect(); autoStealConnection = nil end
	AutoSteal.IsStealing = false
	State.isStealing = false
	_promptCache = nil
	for k,v in pairs(AutoSteal.Data) do if v.ready ~= nil then v.ready = true end end
end

-- ===================================================================
-- CONSTANTES
-- ===================================================================
local MOVE_KEYS = {[Enum.KeyCode.W]=true,[Enum.KeyCode.A]=true,[Enum.KeyCode.S]=true,[Enum.KeyCode.D]=true,[Enum.KeyCode.Up]=true,[Enum.KeyCode.Left]=true,[Enum.KeyCode.Down]=true,[Enum.KeyCode.Right]=true}
local POS = {
	L1=Vector3.new(-476.48,-6.28,92.73), L2=Vector3.new(-483.12,-4.95,94.80),
	R1=Vector3.new(-476.16,-6.52,25.62), R2=Vector3.new(-483.04,-5.09,23.14),
}
local Conns = {antiRag=nil, autoLeft=nil, autoRight=nil, anchor={}}

-- ===================================================================
-- PROXY PART (technique anti-rollback de SZG)
-- Au lieu de modifier la vélocité du HRP (rollback serveur),
-- on crée un Part invisible soudé au HRP. On modifie sa vélocité
-- et le weld entraîne le personnage sans déclencher l'anti-cheat.
-- ===================================================================
local proxy = nil
local function ensureProxy()
	local char = LP.Character
	if not char then return nil end
	local hrp = char:FindFirstChild("HumanoidRootPart")
	if not hrp then return nil end
	if proxy and proxy.Parent == char then return proxy end
	if proxy then pcall(function() proxy:Destroy() end) end
	proxy = Instance.new("Part")
	proxy.Name = DISC.name()
	proxy.Size = Vector3.new(1,1,1)
	proxy.Transparency = 1
	proxy.CanCollide = false
	proxy.Massless = true
	proxy.Parent = char
	local weld = Instance.new("Weld")
	weld.Part0 = hrp
	weld.Part1 = proxy
	weld.C0 = CFrame.new(0,0,0)
	weld.Parent = proxy
	return proxy
end

local function proxyMove(dir, speed)
	local char = LP.Character; if not char then return end
	local hum = char:FindFirstChildOfClass("Humanoid")
	local p = ensureProxy()
	-- Humanoid:Move() pour la direction + proxy pour la vitesse
	-- On évite de toucher hrp.AssemblyLinearVelocity directement (détectable)
	if hum then hum:Move(dir, false) end
	if p then
		p.AssemblyLinearVelocity = Vector3.new(dir.X*speed, p.AssemblyLinearVelocity.Y, dir.Z*speed)
	end
end

local function proxyStop()
	local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
	if hum then hum:Move(Vector3.zero, false) end
	if proxy then proxy.AssemblyLinearVelocity = Vector3.new(0, proxy.AssemblyLinearVelocity.Y, 0) end
end

-- ===================================================================
-- PALETTE (theme eclipse de sang : noir + rouge incandescent)
-- ===================================================================
local C_BG      = Color3.fromRGB(10,6,7)
local C_PANEL   = Color3.fromRGB(18,10,11)
local C_ROW     = Color3.fromRGB(26,14,15)
local C_BORDER  = Color3.fromRGB(70,24,26)
local C_BORDER2 = Color3.fromRGB(110,36,38)
local C_HEADER  = Color3.fromRGB(14,8,9)
local C_ACCENT  = Color3.fromRGB(255,205,200)
local C_ACCENT2 = Color3.fromRGB(255,110,95)
local C_DIM     = Color3.fromRGB(120,78,78)
local C_WHITE   = Color3.fromRGB(255,255,255)
local C_ON_BG   = Color3.fromRGB(120,25,28)
local C_OFF_BG  = Color3.fromRGB(28,14,15)
local C_YSLEM   = Color3.fromRGB(225,35,40)
local C_PINK    = Color3.fromRGB(255,95,55)

-- ===================================================================
-- AUTO-SAVE CONFIG
-- ===================================================================
local saveDebounce = false
local MobileButtons = {Visible=true, Locked=false, Containers={}, Buttons={}}
local _anyKeyListening = false  -- bloque les keybinds pendant l'écoute

autoSaveConfig = function()
	if saveDebounce then return end
	saveDebounce = true
	task.delay(0.5, function()
		local cfg = {
			normalSpeed=State.normalSpeed, carrySpeed=State.carrySpeed,
			laggerSpeed=State.laggerSpeed, laggerCarrySpeed=State.laggerCarrySpeed,
			speedType=State.speedType, laggerActive=State.laggerActive, laggerCarryActive=State.laggerCarryActive,
			autoBatToggled=State.autoBatToggled,
			autoLeftEnabled=State.autoLeftEnabled, autoRightEnabled=State.autoRightEnabled,
			autoStealEnabled=AutoSteal.Enabled, grabRadius=AutoSteal.Radius, grabDuration=AutoSteal.Duration,
			autoCarryOnGrab=State.autoCarryOnGrab,
			infJump=State.infJumpEnabled, infJumpMode=State.infJumpMode,
			antiRagdoll=State.antiRagdollEnabled, fpsBoost=State.fpsBoostEnabled,
			medusaCounter=State.medusaCounterEnabled, unwalkEnabled=State.unwalkEnabled,
			autoTpDown=State.autoTpDownEnabled, autoTpDownY=State.autoTpDownY,
			laggerUseEnabled=State.laggerUseEnabled, laggerUseAmount=State.laggerUseAmount,
			mobileVisible=MobileButtons.Visible, mobileLocked=MobileButtons.Locked,
			keyAutoLeft=State.keyAutoLeft.Name, keyAutoRight=State.keyAutoRight.Name,
			keyDropBR=State.keyDropBR.Name, keyTpDown=State.keyTpDown.Name,
			keyAutoBat=State.keyAutoBat.Name, keyCarrySpeed=State.keyCarrySpeed.Name,
			keyTpUp=State.keyTpUp.Name, keyLaggerMode=State.keyLaggerMode.Name,
			keyInstaReset=State.keyInstaReset.Name,
			keyLaggerUse=State.keyLaggerUse.Name,
			fovEnabled=fovEnabled, fovValue=fovValue,
		}
		pcall(function() writefile("rbxdata_cfg9x.json", HttpService:JSONEncode(cfg)) end)
		saveDebounce = false
	end)
end

-- ===================================================================
-- SPEED HELPERS
-- ===================================================================
local function deactivateAllSpeedModes()
	if State.speedType == "carry" then State.speedType="normal"; if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(false) end end
	if State.laggerActive then State.laggerActive=false; if MobileButtons.Buttons.lagger then MobileButtons.Buttons.lagger(false) end end
	if State.laggerCarryActive then State.laggerCarryActive=false; if MobileButtons.Buttons.laggerCarry then MobileButtons.Buttons.laggerCarry(false) end end
	if State.laggerUseEnabled then State.laggerUseEnabled=false; if MobileButtons.Buttons.laggerUse then MobileButtons.Buttons.laggerUse(false) end end
end

local function toggleSpeedType()
	State._carryManualUntil = tick() + 2.5  -- verrou : l'auto-detect ne touche pas pendant 2.5s
	if State.speedType == "carry" then
		State.speedType = "normal"; refreshUIToggles(); autoSaveConfig()
		if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(false) end; return
	end
	deactivateAllSpeedModes()
	State.speedType = "carry"; refreshUIToggles(); autoSaveConfig()
	if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(true) end
end

local function toggleLagger()
	if State.laggerActive then
		State.laggerActive=false
		refreshUIToggles(); autoSaveConfig()
		if MobileButtons.Buttons.lagger then MobileButtons.Buttons.lagger(false) end; return
	end
	deactivateAllSpeedModes()
	State.laggerActive=true
	refreshUIToggles(); autoSaveConfig()
	if MobileButtons.Buttons.lagger then MobileButtons.Buttons.lagger(true) end
end

local function toggleLaggerCarry()
	if State.laggerCarryActive then
		State.laggerCarryActive=false
		refreshUIToggles(); autoSaveConfig()
		if MobileButtons.Buttons.laggerCarry then MobileButtons.Buttons.laggerCarry(false) end; return
	end
	deactivateAllSpeedModes()
	State.laggerCarryActive=true
	refreshUIToggles(); autoSaveConfig()
	if MobileButtons.Buttons.laggerCarry then MobileButtons.Buttons.laggerCarry(true) end
end

local function toggleLaggerUse()
	if State.laggerUseEnabled then
		State.laggerUseEnabled=false
		refreshUIToggles(); autoSaveConfig()
		if MobileButtons.Buttons.laggerUse then MobileButtons.Buttons.laggerUse(false) end; return
	end
	deactivateAllSpeedModes()
	State.laggerUseEnabled=true
	refreshUIToggles(); autoSaveConfig()
	if MobileButtons.Buttons.laggerUse then MobileButtons.Buttons.laggerUse(true) end
end

local function getCurrentSpeed()
	if State.laggerUseEnabled then return State.laggerUseAmount end
	if State.laggerCarryActive then return State.laggerCarrySpeed
	elseif State.laggerActive then return State.laggerCarrySpeed
	else return State.speedType == "normal" and State.normalSpeed or State.carrySpeed end
end

local function getAutoMoveSpeed()
	if State.laggerCarryActive then return State.normalSpeed
	elseif State.laggerActive then return State.laggerSpeed
	else return State.normalSpeed end
end

-- ===================================================================
-- FOV MODIFIER
-- ===================================================================
local fovEnabled = false
local fovValue   = 70
local fovConn    = nil

local function enableFOV()
	fovEnabled = true
	if fovConn then fovConn:Disconnect() end
	fovConn = RunService.RenderStepped:Connect(function()
		if not fovEnabled then fovConn:Disconnect(); fovConn = nil; return end
		pcall(function() workspace.CurrentCamera.FieldOfView = fovValue end)
	end)
	if UIR.fovWidget then UIR.fovWidget.Visible = true; if UIR.fovValLbl then UIR.fovValLbl.Text = tostring(fovValue) end end
end

local function disableFOV()
	fovEnabled = false
	if fovConn then fovConn:Disconnect(); fovConn = nil end
	pcall(function() workspace.CurrentCamera.FieldOfView = 70 end)
	if UIR.fovWidget then UIR.fovWidget.Visible = false end
end

-- Discord
local DISCORD_LINK = "discord.gg/mJN8wTQqEv"

-- ===================================================================
-- SPEED BYPASS (Lag self-freeze + Speed, style Ice Hub)
-- ===================================================================
local _sbMobile = UIS.TouchEnabled and not UIS.MouseEnabled
-- TableIncrease (profondeur) + delay adaptatifs : mobile = plus gros/plus lent, PC = plus fin/rapide
local SB = {running=false, thread=nil, bomb=nil, mode=(_sbMobile and "Mobile" or "PC"), pcPower=265, mobPower=290, spamDelay=(_sbMobile and 0.85 or 0.05), DEPTH=186, widget=nil, statusLbl=nil, pill=nil, pillStk=nil, ball=nil, powerBox=nil, keybind=Enum.KeyCode.V, listening=false,
	lagOn=false, speedOn=false, lagAmount=0.12, speedAmount=30, speedConn=nil, lagConn=nil}
-- Speed Ice Hub : vitesse quand on bouge (sur RenderStepped, comme la source)
function SB.startSpeed()
	if SB.speedConn then return end
	SB.speedConn = RunService.RenderStepped:Connect(function()
		if not SB.speedOn then return end
		local char = LP.Character
		local hrp = char and char:FindFirstChild("HumanoidRootPart")
		local hum = char and char:FindFirstChildOfClass("Humanoid")
		if not hrp or not hum then return end
		if hum.MoveDirection.Magnitude > 0 then
			local dir = hum.MoveDirection.Unit
			hrp.AssemblyLinearVelocity = Vector3.new(dir.X*SB.speedAmount, hrp.AssemblyLinearVelocity.Y, dir.Z*SB.speedAmount)
		end
	end)
end
function SB.stopSpeed()
	if SB.speedConn then SB.speedConn:Disconnect(); SB.speedConn = nil end
end
-- Lag Ice Hub : self-freeze (boucle bloquante) - via task.spawn pour ne pas casser le hub
function SB.startLag()
	if SB.lagConn then return end
	SB.lagConn = true
	task.spawn(function()
		while SB.lagOn do
			if SB.lagAmount > 0 then
				local t = tick()
				while tick() - t < SB.lagAmount do end
			end
			RunService.RenderStepped:Wait()
		end
		SB.lagConn = nil
	end)
end
function SB.stopLag()
	SB.lagOn = false
	SB.lagConn = nil
end

-- ===================================================================
-- ANTI BAT - Fling defensif (puissance en %, 100% = 5000)
-- ===================================================================
local AB = {active=false, conn=nil, pct=20, MAXPOWER=5000, widget=nil, statusLbl=nil, pill=nil, pillStk=nil, ball=nil, box=nil, FORCE=1000}
function AB.power() return AB.FORCE end
function AB.start()
	if AB.conn then AB.conn:Disconnect() end
	AB.conn = RunService.Heartbeat:Connect(function()
		if not AB.active then return end
		local c = LP.Character
		local root = c and c:FindFirstChild("HumanoidRootPart")
		if not root or not root.Parent then return end
		local orig = root.Velocity
		root.Velocity = Vector3.new(AB.FORCE, root.Velocity.Y, AB.FORCE)
		RunService.RenderStepped:Wait()
		local r2 = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
		if r2 and r2.Parent then r2.Velocity = orig end
	end)
end
function AB.stop()
	if AB.conn then AB.conn:Disconnect(); AB.conn = nil end
end
function AB.refresh()
	if not AB.statusLbl then return end
	local on = AB.active
	AB.statusLbl.Text = on and "ENABLED" or "DISABLED"
	AB.statusLbl.TextColor3 = on and Color3.fromRGB(255,110,95) or Color3.fromRGB(150,95,95)
	if AB.pill then TweenService:Create(AB.pill, TweenInfo.new(0.2), {BackgroundColor3 = on and Color3.fromRGB(120,25,28) or Color3.fromRGB(22,22,28)}):Play() end
	if AB.pillStk then TweenService:Create(AB.pillStk, TweenInfo.new(0.2), {Color = on and Color3.fromRGB(255,110,95) or Color3.fromRGB(80,28,30)}):Play() end
	if AB.ball then TweenService:Create(AB.ball, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = on and UDim2.new(1,-15,0.5,-5) or UDim2.new(0,4,0.5,-5), BackgroundColor3 = on and Color3.fromRGB(255,255,255) or Color3.fromRGB(110,80,80)}):Play() end
end
function AB.toggle()
	AB.active = not AB.active
	if AB.active then AB.start() else AB.stop() end
	AB.refresh()
end
if LP then LP.CharacterAdded:Connect(function() if AB.active then AB.stop(); task.wait(0.2); AB.start() end end) end

-- ===================================================================
-- INSTA RESET (source Chiraq Hub)
-- Hook le remote "RE/" pour reset le balloon jump adverse
-- ===================================================================
local RESET_GUID     = "f888ee6e-c86d-46e1-93d7-0639d6635d42"
local resetRemote    = nil
local resetCooldown  = false

-- Hook FireServer pour trouver le remote automatiquement
pcall(function()
	local orig
	orig = hookfunction(Instance.new("RemoteEvent").FireServer, newcclosure(function(self, ...)
		if not resetRemote and type(self.Name) == "string" and self.Name:sub(1,3) == "RE/" then
			resetRemote = self
		end
		return orig(self, ...)
	end))
end)

local function instaReset()
	if resetCooldown then return end
	if not resetRemote then
		-- Chercher dans tout le jeu (plus robuste que juste ReplicatedStorage)
		for _, obj in ipairs(game:GetDescendants()) do
			if obj:IsA("RemoteEvent") and obj.Name:sub(1,3) == "RE/" then
				resetRemote = obj; break
			end
		end
		if not resetRemote then return end
	end
	resetCooldown = true
	local character = LP.Character
	local humanoid = character and character:FindFirstChildOfClass("Humanoid")
	-- Si deja mort, un seul fire suffit
	if humanoid and humanoid.Health <= 0 then
		pcall(function() resetRemote:FireServer(RESET_GUID, LP, "balloon") end)
		resetCooldown = false
		return
	end
	-- Detecter le respawn pour arreter le spam (Died / Health / retire du jeu)
	local resetDetected = false
	local conns = {}
	if humanoid then
		table.insert(conns, humanoid.Died:Connect(function() resetDetected = true end))
		table.insert(conns, humanoid:GetPropertyChangedSignal("Health"):Connect(function() if humanoid.Health <= 0 then resetDetected = true end end))
	end
	if character then table.insert(conns, character.AncestryChanged:Connect(function(_, parent) if not parent then resetDetected = true end end)) end
	task.spawn(function()
		for _ = 1, 50 do
			if resetDetected then break end
			pcall(function() resetRemote:FireServer(RESET_GUID, LP, "balloon") end)
			task.wait()
		end
		for _, conn in ipairs(conns) do pcall(function() conn:Disconnect() end) end
		resetCooldown = false
	end)
end

-- ===================================================================
-- TP DOWN
-- ===================================================================
local function tpToGround()
	local char = LP.Character; if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
	local rot = root.CFrame.Rotation
	local params = RaycastParams.new()
	params.FilterType = Enum.RaycastFilterType.Exclude
	params.FilterDescendantsInstances = {char}
	local res = workspace:Raycast(root.Position, Vector3.new(0,-500,0), params)
	if res then root.CFrame = CFrame.new(res.Position + Vector3.new(0,3,0)) * rot
	else root.CFrame = CFrame.new(root.Position + Vector3.new(0,-20,0)) * rot end
end

local function tpUp()
	local char = LP.Character; if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
	local hum = char:FindFirstChildOfClass("Humanoid")
	-- Monte de 8 studs seulement (pas trop haut pour éviter le reset anti-cheat)
	local newPos = root.Position + Vector3.new(0, 10, 0)
	root.CFrame = CFrame.new(newPos) * root.CFrame.Rotation
	root.AssemblyLinearVelocity = Vector3.zero
	if hum then hum:ChangeState(Enum.HumanoidStateType.Running) end
end

-- ===================================================================
-- DROP BRAINROT
-- ===================================================================
-- Drop Brainrot = fling (technique Phantom Duels : oscillation velocity x10000)
local function runDropBrainrot()
	if State.dropBrainrotActive then return end
	if batAimbotEnabled then stopBatAimbot(); if UIR.setAutoBat then UIR.setAutoBat(false) end end
	State.dropBrainrotActive = true
	local conns = {}
	-- desactive les collisions des autres joueurs (pour les traverser/fling)
	local colConn = RunService.Stepped:Connect(function()
		if not State.dropBrainrotActive then return end
		for _, p in ipairs(Players:GetPlayers()) do
			if p ~= LP and p.Character then
				for _, part in ipairs(p.Character:GetChildren()) do
					if part:IsA("BasePart") then part.CanCollide = false end
				end
			end
		end
	end)
	table.insert(conns, colConn)
	local flingThread = coroutine.create(function()
		while State.dropBrainrotActive do
			RunService.Heartbeat:Wait()
			local c = LP.Character
			local root = c and c:FindFirstChild("HumanoidRootPart")
			if not root then break end
			local vel = root.Velocity
			root.Velocity = vel*10000 + Vector3.new(0,10000,0)
			RunService.RenderStepped:Wait()
			if root and root.Parent then root.Velocity = vel end
			RunService.Stepped:Wait()
			if root and root.Parent then root.Velocity = vel + Vector3.new(0,0.1,0) end
		end
	end)
	table.insert(conns, flingThread)
	coroutine.resume(flingThread)
	task.delay(0.1, function()
		State.dropBrainrotActive = false
		for _, c in ipairs(conns) do
			if typeof(c) == "RBXScriptConnection" then c:Disconnect()
			elseif type(c) == "thread" then pcall(coroutine.close, c) end
		end
		conns = {}
	end)
end

-- ===================================================================
-- AUTO LEFT / RIGHT
-- ===================================================================
-- ===================================================================
-- AUTO LEFT / RIGHT (rework SZG : proxy + waypoints séquentiels)
-- Waypoints 1-2 : normalSpeed (aller voler)
-- Waypoints 3-4 : carrySpeed (retour avec brainrot)
-- ===================================================================
local leftWaypoints = {
	Vector3.new(-476.85, -6.59, 94.91),
	Vector3.new(-485.55, -4.53, 100.61),
	Vector3.new(-475.60, -6.59, 92.80),
	Vector3.new(-475.26, -6.57, 21.54),
}
local rightWaypoints = {
	Vector3.new(-475.77, -6.57, 26.76),
	Vector3.new(-485.85, -4.48, 20.13),
	Vector3.new(-475.83, -6.59, 26.54),
	Vector3.new(-476.17, -6.09, 97.73),
}

local patrolConnection = nil
local patrolWaypoints = nil
local patrolIndex = 1
local patrolFrozen = false
local patrolFreezeUntil = 0
local PATROL_WAIT_AT_BASE = 0.7

local function patrolMoveTo(target, speed)
	local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
	if not hrp then return end
	local dir = (target - hrp.Position)
	local moveDir = Vector3.new(dir.X, 0, dir.Z).Unit
	local hum = LP.Character:FindFirstChildOfClass("Humanoid")
	if hum then hum:Move(moveDir, false) end
	proxyMove(moveDir, speed)
end

startAutoLeft = function()
	stopAutoLeft(); stopAutoRight()
	State.autoLeftPhase = 1; patrolIndex = 1
	patrolFrozen = false; patrolFreezeUntil = 0
	patrolWaypoints = leftWaypoints; ensureProxy()
	State.speedType = "normal"
	if refreshUIToggles then refreshUIToggles() end
	if MobileButtons.Buttons and MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(false) end
	patrolConnection = RunService.Stepped:Connect(function()
		if not State.autoLeftEnabled or not patrolWaypoints then return end
		local char = LP.Character; if not char then return end
		local hrp = char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
		-- Pause de 2s à WP2 (devant la base ennemie)
		if patrolFrozen then
			proxyStop()
			if tick() >= patrolFreezeUntil then
				patrolFrozen = false
				patrolIndex = patrolIndex + 1
			end
			return
		end
		local target = patrolWaypoints[patrolIndex]; if not target then return end
		local dist = (target - hrp.Position).Magnitude
		local speed = (patrolIndex <= 2) and State.normalSpeed or State.carrySpeed
		if dist < 2.5 then
			if patrolIndex == 2 then
				-- Arrivé devant la base → on attend PATROL_WAIT_AT_BASE secondes
				patrolFrozen = true
				patrolFreezeUntil = tick() + PATROL_WAIT_AT_BASE
				proxyStop()
				return
			end
			patrolIndex = patrolIndex + 1
			if patrolIndex > #patrolWaypoints then
				proxyStop(); State.autoLeftEnabled = false
				if patrolConnection then patrolConnection:Disconnect(); patrolConnection = nil end
				patrolWaypoints = nil; patrolIndex = 1
				if UIR.setAutoLeft then UIR.setAutoLeft(false) end
				if MobileButtons.Buttons.autoLeft then MobileButtons.Buttons.autoLeft(false) end
				if State.autoCarryOnGrab then
					State.speedType = "carry"
					if refreshUIToggles then refreshUIToggles() end
					if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(true) end
					autoSaveConfig()
				end
				return
			end
		else
			patrolMoveTo(target, speed)
		end
	end)
end
stopAutoLeft = function()
	if patrolConnection and State.autoLeftEnabled then patrolConnection:Disconnect(); patrolConnection = nil end
	patrolWaypoints = nil; patrolIndex = 1; patrolFrozen = false; proxyStop()
	State.autoLeftPhase = 1
	if MobileButtons.Buttons.autoLeft then MobileButtons.Buttons.autoLeft(false) end
end

startAutoRight = function()
	stopAutoRight(); stopAutoLeft()
	State.autoRightPhase = 1; patrolIndex = 1
	patrolFrozen = false; patrolFreezeUntil = 0
	patrolWaypoints = rightWaypoints; ensureProxy()
	State.speedType = "normal"
	if refreshUIToggles then refreshUIToggles() end
	if MobileButtons.Buttons and MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(false) end
	patrolConnection = RunService.Stepped:Connect(function()
		if not State.autoRightEnabled or not patrolWaypoints then return end
		local char = LP.Character; if not char then return end
		local hrp = char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
		-- Pause de 2s à WP2
		if patrolFrozen then
			proxyStop()
			if tick() >= patrolFreezeUntil then
				patrolFrozen = false
				patrolIndex = patrolIndex + 1
			end
			return
		end
		local target = patrolWaypoints[patrolIndex]; if not target then return end
		local dist = (target - hrp.Position).Magnitude
		local speed = (patrolIndex <= 2) and State.normalSpeed or State.carrySpeed
		if dist < 2.5 then
			if patrolIndex == 2 then
				patrolFrozen = true
				patrolFreezeUntil = tick() + PATROL_WAIT_AT_BASE
				proxyStop()
				return
			end
			patrolIndex = patrolIndex + 1
			if patrolIndex > #patrolWaypoints then
				proxyStop(); State.autoRightEnabled = false
				if patrolConnection then patrolConnection:Disconnect(); patrolConnection = nil end
				patrolWaypoints = nil; patrolIndex = 1
				if UIR.setAutoRight then UIR.setAutoRight(false) end
				if MobileButtons.Buttons.autoRight then MobileButtons.Buttons.autoRight(false) end
				if State.autoCarryOnGrab then
					State.speedType = "carry"
					if refreshUIToggles then refreshUIToggles() end
					if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(true) end
					autoSaveConfig()
				end
				return
			end
		else
			patrolMoveTo(target, speed)
		end
	end)
end
stopAutoRight = function()
	if patrolConnection and State.autoRightEnabled then patrolConnection:Disconnect(); patrolConnection = nil end
	patrolWaypoints = nil; patrolIndex = 1; patrolFrozen = false; proxyStop()
	State.autoRightPhase = 1
	if MobileButtons.Buttons.autoRight then MobileButtons.Buttons.autoRight(false) end
end

-- ===================================================================
-- ANTI RAGDOLL
-- ===================================================================
startAntiRagdoll = function()
	if Conns.antiRag then return end
	local _t = 0
	Conns.antiRag = RunService.Heartbeat:Connect(function()
		local now = tick(); if now-_t < 0.1 then return end; _t = now
		local char = LP.Character; if not char then return end
		local hum2 = char:FindFirstChildOfClass("Humanoid"); local root = char:FindFirstChild("HumanoidRootPart")
		if hum2 then
			local st = hum2:GetState()
			if st==Enum.HumanoidStateType.Physics or st==Enum.HumanoidStateType.Ragdoll or st==Enum.HumanoidStateType.FallingDown then
				hum2:ChangeState(Enum.HumanoidStateType.Running)
				workspace.CurrentCamera.CameraSubject = hum2
				pcall(function() local pm=LP.PlayerScripts:FindFirstChild("PlayerModule"); if pm then require(pm:FindFirstChild("ControlModule")):Enable() end end)
				if root then root.Velocity=Vector3.zero; root.RotVelocity=Vector3.zero end
			end
		end
		for _,obj in ipairs(char:GetDescendants()) do if obj:IsA("Motor6D") and not obj.Enabled then obj.Enabled=true end end
	end)
end
stopAntiRagdoll = function() if Conns.antiRag then Conns.antiRag:Disconnect(); Conns.antiRag=nil end end

-- ===================================================================
-- FPS BOOST
-- ===================================================================
applyFPSBoost = function()
	pcall(function() setfpscap(999999999) end)
	local function processObj(v)
		pcall(function()
			if v:IsA("Model") then v.LevelOfDetail=Enum.ModelLevelOfDetail.Disabled
			elseif v:IsA("MeshPart") then v.CastShadow=false; v.RenderFidelity=Enum.RenderFidelity.Performance
			elseif v:IsA("BasePart") then v.CastShadow=false; v.Material=Enum.Material.Plastic; v.Reflectance=0
			elseif v:IsA("Decal") or v:IsA("Texture") then v.Transparency=1
			elseif v:IsA("SpecialMesh") then v.TextureId=""
			elseif v:IsA("Fire") or v:IsA("SpotLight") or v:IsA("Smoke") or v:IsA("Sparkles") or v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") then v.Enabled=false
			elseif v:IsA("SurfaceAppearance") then v:Destroy() end
		end)
	end
	for _,v in pairs(workspace:GetDescendants()) do processObj(v) end
	pcall(function()
		local lighting = game:GetService("Lighting")
		for _,v in pairs(lighting:GetDescendants()) do
			pcall(function()
				if v:IsA("Sky") or v:IsA("Atmosphere") or v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("ColorCorrectionEffect") then v:Destroy() end
			end)
		end
		lighting.GlobalShadows = false; lighting.FogEnd = 9e9; lighting.Brightness = 0
	end)
	workspace.DescendantAdded:Connect(function(v) if State.fpsBoostEnabled then task.spawn(processObj,v) end end)
end

-- ===================================================================
-- MEDUSA COUNTER
-- ===================================================================
local function findMedusa()
	local char = LP.Character; if not char then return nil end
	for _,tool in ipairs(char:GetChildren()) do
		if tool:IsA("Tool") then local tn=tool.Name:lower()
			if tn:find("medusa") or tn:find("head") or tn:find("stone") then return tool end
		end
	end
	local bp = LP:FindFirstChild("Backpack")
	if bp then for _,tool in ipairs(bp:GetChildren()) do
		if tool:IsA("Tool") then local tn=tool.Name:lower()
			if tn:find("medusa") or tn:find("head") or tn:find("stone") then return tool end
		end
	end end
	return nil
end

local function useMedusaCounter()
	if State.medusaDebounce then return end
	if tick()-State.medusaLastUsed < 25 then return end
	local char = LP.Character; if not char then return end
	State.medusaDebounce = true
	local med = findMedusa()
	if not med then State.medusaDebounce=false; return end
	if med.Parent ~= char then local hum2=char:FindFirstChildOfClass("Humanoid"); if hum2 then hum2:EquipTool(med) end end
	pcall(function() med:Activate() end)
	State.medusaLastUsed = tick()
	State.medusaDebounce = false
end

local function onAnchorChanged(part)
	return part:GetPropertyChangedSignal("Anchored"):Connect(function()
		if part.Anchored and part.Transparency == 1 then
			-- Medusa detectee : auto insta reset si le toggle est active
			-- (mais pas si le Bat Counter tourne, sinon il reset quand on se fait taper)
			if State.medusaAutoReset and not BC.active then instaReset() end
			-- Medusa counter seulement si l'option est activee
			if State.medusaCounterEnabled then useMedusaCounter() end
		end
	end)
end

setupMedusaCounter = function(char)
	stopMedusaCounter(); if not char then return end
	for _,part in ipairs(char:GetDescendants()) do
		if part:IsA("BasePart") then table.insert(Conns.anchor, onAnchorChanged(part)) end
	end
	table.insert(Conns.anchor, char.DescendantAdded:Connect(function(part)
		if part:IsA("BasePart") then table.insert(Conns.anchor, onAnchorChanged(part)) end
	end))
end
stopMedusaCounter = function()
	for _,c in pairs(Conns.anchor) do pcall(function() c:Disconnect() end) end
	Conns.anchor = {}
end

-- ===================================================================
-- UNWALK
-- ===================================================================
local savedAnimate = nil
local function startUnwalk()
	if State.unwalkEnabled then return end
	State.unwalkEnabled = true
	local c = LP.Character; if not c then return end
	local hum = c:FindFirstChildOfClass("Humanoid")
	if hum then for _,t in ipairs(hum:GetPlayingAnimationTracks()) do t:Stop() end end
	local anim = c:FindFirstChild("Animate")
	if anim then savedAnimate = anim:Clone(); anim:Destroy() end
end
local function stopUnwalk()
	if not State.unwalkEnabled then return end
	State.unwalkEnabled = false
	local c = LP.Character
	if c and savedAnimate then
		savedAnimate.Parent = c; savedAnimate.Disabled = false; savedAnimate = nil
	end
end

-- ===================================================================
-- ANTI LAG (boost FPS : enleve ombres, brouillard, particules, basse qualite)
-- ===================================================================
-- ===================================================================
-- INF JUMP (toggle independant, methode Irish : JumpRequest + ChangeState)
-- ===================================================================
local IJ = {active=false, conn=nil}
function IJ.start()
	if IJ.conn then IJ.conn:Disconnect() end
	if IJ.hbConn then IJ.hbConn:Disconnect() end
	-- Methode M5 : capture le saut dans un flag, applique l'impulsion dans le
	-- Heartbeat (pas sur le JumpRequest direct) => cohabite avec l'Anti Bat sans reset
	IJ.wantJump = false
	IJ.conn = UIS.JumpRequest:Connect(function()
		if IJ.active then IJ.wantJump = true end
	end)
	IJ.hbConn = RunService.Heartbeat:Connect(function()
		if not IJ.active or not IJ.wantJump then return end
		IJ.wantJump = false
		local c = LP.Character
		local root = c and c:FindFirstChild("HumanoidRootPart")
		if root then
			local v = root.AssemblyLinearVelocity
			root.AssemblyLinearVelocity = Vector3.new(v.X, 50, v.Z)
		end
	end)
end
function IJ.stop()
	if IJ.conn then IJ.conn:Disconnect(); IJ.conn = nil end
	if IJ.hbConn then IJ.hbConn:Disconnect(); IJ.hbConn = nil end
	IJ.wantJump = false
end
function IJ.refresh()
	if not IJ.statusLbl then return end
	local on = IJ.active
	IJ.statusLbl.Text = on and "ENABLED" or "DISABLED"
	IJ.statusLbl.TextColor3 = on and Color3.fromRGB(255,110,95) or Color3.fromRGB(150,95,95)
	if IJ.pill then TweenService:Create(IJ.pill, TweenInfo.new(0.2), {BackgroundColor3 = on and Color3.fromRGB(120,25,28) or Color3.fromRGB(22,22,28)}):Play() end
	if IJ.pillStk then TweenService:Create(IJ.pillStk, TweenInfo.new(0.2), {Color = on and Color3.fromRGB(255,110,95) or Color3.fromRGB(80,28,30)}):Play() end
	if IJ.ball then TweenService:Create(IJ.ball, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {Position = on and UDim2.new(1,-15,0.5,-5) or UDim2.new(0,4,0.5,-5), BackgroundColor3 = on and Color3.fromRGB(255,255,255) or Color3.fromRGB(110,80,80)}):Play() end
end
function IJ.toggle()
	IJ.active = not IJ.active
	if IJ.active then IJ.start() else IJ.stop() end
	IJ.refresh()
end

-- ===================== BAT COUNTER (auto-contre quand ragdoll) =====================
BC = {active=false, conn=nil}
BC.batNames = {"Bat","Slap","Iron Slap","Gold Slap","Diamond Slap","Emerald Slap","Ruby Slap","Dark Matter Slap","Flame Slap","Nuclear Slap","Galaxy Slap","Glitched Slap"}
function BC.findBat()
	local c = LP.Character
	if not c then return nil end
	local bp = LP:FindFirstChildOfClass("Backpack")
	for _, n in ipairs(BC.batNames) do
		local t = c:FindFirstChild(n) or (bp and bp:FindFirstChild(n))
		if t then return t end
	end
	for _, ch in ipairs(c:GetChildren()) do
		if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end
	end
	if bp then for _, ch in ipairs(bp:GetChildren()) do
		if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end
	end end
	return nil
end
function BC.nearestAttacker(root)
	local closest, cd = nil, math.huge
	for _, p in ipairs(Players:GetPlayers()) do
		if p ~= LP and p.Character then
			local tr = p.Character:FindFirstChild("HumanoidRootPart")
			if tr then
				local d = (tr.Position - root.Position).Magnitude
				if d < cd then cd = d; closest = tr end
			end
		end
	end
	return closest
end
function BC.start()
	if BC.conn then BC.conn:Disconnect() end
	BC.conn = RunService.Heartbeat:Connect(function()
		if not BC.active then return end
		local c = LP.Character
		local hum = c and c:FindFirstChildOfClass("Humanoid")
		if not hum then return end
		local st = hum:GetState()
		if st == Enum.HumanoidStateType.Physics or st == Enum.HumanoidStateType.Ragdoll or st == Enum.HumanoidStateType.FallingDown then
			local root = c:FindFirstChild("HumanoidRootPart")
			local bat = BC.findBat()
			if not bat then return end
			if bat.Parent ~= c then pcall(function() hum:EquipTool(bat) end) end
			if root then
				local tHRP = BC.nearestAttacker(root)
				if tHRP then
					local dir = (tHRP.Position - root.Position).Unit
					root.CFrame = CFrame.lookAt(root.Position, root.Position + Vector3.new(dir.X,0,dir.Z))
					root.AssemblyLinearVelocity = dir * 75
				end
			end
			pcall(function() bat:Activate() end)
			RunService.Heartbeat:Wait()
			pcall(function() bat:Activate() end)
		end
	end)
end
function BC.stop() if BC.conn then BC.conn:Disconnect(); BC.conn = nil end end

local ANL = {active=false, conn=nil}
function ANL.process(obj)
	pcall(function()
		if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") or obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Sparkles") then
			obj.Enabled = false
		elseif obj:IsA("Decal") or obj:IsA("Texture") then
			obj.Transparency = 1
		elseif obj:IsA("BasePart") then
			obj.Material = Enum.Material.Plastic; obj.Reflectance = 0; obj.CastShadow = false
		end
	end)
end
function ANL.start()
	if ANL.active then return end
	ANL.active = true
	local Lt = game:GetService("Lighting")
	pcall(function()
		Lt.GlobalShadows = false; Lt.FogEnd = 1e10; Lt.Brightness = 1
		Lt.EnvironmentDiffuseScale = 0; Lt.EnvironmentSpecularScale = 0
		for _, e in ipairs(Lt:GetChildren()) do
			if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then e.Enabled = false end
		end
		settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
	end)
	for _, obj in ipairs(workspace:GetDescendants()) do ANL.process(obj) end
	ANL.conn = workspace.DescendantAdded:Connect(function(obj)
		if ANL.active then ANL.process(obj) end
	end)
end
function ANL.stop()
	ANL.active = false
	if ANL.conn then ANL.conn:Disconnect(); ANL.conn = nil end
end

-- ===================================================================
-- HARDER HIT ANIM (remplace les animations du perso, cosmetique local)
-- ===================================================================
local HHA = {enabled=false, conn=nil, saved=nil,
	ids={
		idle1="rbxassetid://133806214992291", idle2="rbxassetid://94970088341563",
		walk="rbxassetid://707897309", run="rbxassetid://707861613",
		jump="rbxassetid://116936326516985", fall="rbxassetid://116936326516985",
		climb="rbxassetid://116936326516985", swim="rbxassetid://116936326516985",
		swimidle="rbxassetid://116936326516985",
	}}
function HHA.save(char)
	local a = char:FindFirstChild("Animate"); if not a then return end
	local function g(o) return o and o.AnimationId or nil end
	HHA.saved = {
		idle1=g(a.idle and a.idle.Animation1), idle2=g(a.idle and a.idle.Animation2),
		walk=g(a.walk and a.walk.WalkAnim), run=g(a.run and a.run.RunAnim),
		jump=g(a.jump and a.jump.JumpAnim), fall=g(a.fall and a.fall.FallAnim),
		climb=g(a.climb and a.climb.ClimbAnim), swim=g(a.swim and a.swim.Swim),
		swimidle=g(a.swimidle and a.swimidle.SwimIdle),
	}
end
function HHA.apply(char)
	local a = char:FindFirstChild("Animate"); if not a then return end
	local function s(o,id) if o then o.AnimationId = id end end
	s(a.idle and a.idle.Animation1, HHA.ids.idle1); s(a.idle and a.idle.Animation2, HHA.ids.idle2)
	s(a.walk and a.walk.WalkAnim, HHA.ids.walk); s(a.run and a.run.RunAnim, HHA.ids.run)
	s(a.jump and a.jump.JumpAnim, HHA.ids.jump); s(a.fall and a.fall.FallAnim, HHA.ids.fall)
	s(a.climb and a.climb.ClimbAnim, HHA.ids.climb); s(a.swim and a.swim.Swim, HHA.ids.swim)
	s(a.swimidle and a.swimidle.SwimIdle, HHA.ids.swimidle)
end
function HHA.restore(char)
	if not HHA.saved then return end
	local a = char:FindFirstChild("Animate"); if not a then return end
	local function s(o,id) if o and id then o.AnimationId = id end end
	s(a.idle and a.idle.Animation1, HHA.saved.idle1); s(a.idle and a.idle.Animation2, HHA.saved.idle2)
	s(a.walk and a.walk.WalkAnim, HHA.saved.walk); s(a.run and a.run.RunAnim, HHA.saved.run)
	s(a.jump and a.jump.JumpAnim, HHA.saved.jump); s(a.fall and a.fall.FallAnim, HHA.saved.fall)
	s(a.climb and a.climb.ClimbAnim, HHA.saved.climb); s(a.swim and a.swim.Swim, HHA.saved.swim)
	s(a.swimidle and a.swimidle.SwimIdle, HHA.saved.swimidle)
	local hum = char:FindFirstChildOfClass("Humanoid")
	if hum then for _,t in ipairs(hum:GetPlayingAnimationTracks()) do t:Stop(0) end end
end
function HHA.start()
	if HHA.conn then HHA.conn:Disconnect(); HHA.conn = nil end
	local char = LP.Character
	if char then
		HHA.save(char); HHA.apply(char)
		local hum = char:FindFirstChildOfClass("Humanoid")
		if hum then for _,t in ipairs(hum:GetPlayingAnimationTracks()) do t:Stop(0) end end
	end
	HHA.conn = RunService.Heartbeat:Connect(function()
		if not HHA.enabled then return end
		local c = LP.Character
		if c then HHA.apply(c) end
	end)
end
function HHA.stop()
	if HHA.conn then HHA.conn:Disconnect(); HHA.conn = nil end
	local char = LP.Character
	if char then HHA.restore(char) end
end

-- ===================================================================
-- BAT AIMBOT (logique source exacte : flyToFront + hrp.Velocity direct)
-- ===================================================================
local batAimbotEnabled = false
local aimbotConn = nil
local hittingCooldown = false

local aimbotHighlight = Instance.new("Highlight")
aimbotHighlight.Name = DISC.name()
aimbotHighlight.FillColor = Color3.fromRGB(168, 85, 247)
aimbotHighlight.OutlineColor = Color3.fromRGB(255, 255, 255)
aimbotHighlight.FillTransparency = 0.5
aimbotHighlight.OutlineTransparency = 0
DISC.hide(aimbotHighlight)

local function getBat()
	local char = LP.Character; if not char then return nil end
	local bp = LP:FindFirstChild("Backpack")
	local SlapList = {"Bat","Slap","Iron Slap","Gold Slap","Diamond Slap","Emerald Slap","Ruby Slap","Dark Matter Slap","Flame Slap","Nuclear Slap","Galaxy Slap","Glitched Slap"}
	local tool = char:FindFirstChild("Bat")
	if tool then return tool end
	if bp then tool = bp:FindFirstChild("Bat"); if tool then tool.Parent = char; return tool end end
	for _, name in ipairs(SlapList) do
		local t = char:FindFirstChild(name) or (bp and bp:FindFirstChild(name))
		if t then return t end
	end
end

local function tryHitBat()
	if hittingCooldown then return end
	hittingCooldown = true
	local bat = getBat()
	if bat then
		pcall(function()
			bat:Activate()
			local evt = bat:FindFirstChildWhichIsA("RemoteEvent")
			if evt then evt:FireServer() end
		end)
	end
	task.delay(0.12, function() hittingCooldown = false end)
end

-- Constantes + etat aimbot dans 1 table (economie de locals)
-- SPEED/VERT = vitesse de deplacement, DIST/HEIGHT = position de frappe,
-- V_OFF = vise un peu plus haut, TURN/MAX_TURN = rotation lissee
local AIM = {SPEED=58, VERT=52, DIST=-2.8, HEIGHT=4.75, V_OFF=1, TURN=285, MAX_TURN=28, target=nil, lastScan=0}

-- Cible valide : vivante + pas de ForceField (ignore les joueurs proteges/spawn)
function AIM.valid(targetRoot)
	if not targetRoot or not targetRoot.Parent then return false end
	local char = targetRoot.Parent
	local hum = char:FindFirstChildOfClass("Humanoid")
	local ff = char:FindFirstChildOfClass("ForceField")
	return hum and hum.Health > 0 and not ff
end

-- Cible avec target lock (reste sur la cible tant qu'elle est valide) + ignore ForceField
local function getAutoBatTarget()
	local char = LP.Character
	local root = char and char:FindFirstChild("HumanoidRootPart")
	if not root then return nil end
	-- Target lock : garder la cible actuelle tant qu'elle est valide
	if AIM.target and AIM.valid(AIM.target) then return AIM.target end
	AIM.lastScan = tick()
	AIM.target = nil
	local closest, minDist = nil, math.huge
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= LP and plr.Character then
			local tRoot = plr.Character:FindFirstChild("HumanoidRootPart")
			if tRoot and AIM.valid(tRoot) then
				local dist = (tRoot.Position - root.Position).Magnitude
				if dist < minDist then minDist = dist; closest = tRoot end
			end
		end
	end
	AIM.target = closest
	return AIM.target
end

local function startBatAimbot()
	if aimbotConn then return end
	batAimbotEnabled = true; State.autoBatToggled = true
	aimbotConn = RunService.Heartbeat:Connect(function()
		if not batAimbotEnabled then return end
		local char = LP.Character
		local root = char and char:FindFirstChild("HumanoidRootPart")
		if not root then return end
		local target = getAutoBatTarget()
		if target and target.Parent then
			aimbotHighlight.Adornee = target.Parent
			-- PREDICTION : anticipe ou sera la cible selon sa vitesse
			local tVel = target.AssemblyLinearVelocity
			local predict = math.clamp(tVel.Magnitude / 130, 0.05, 0.18)
			local predictedPos = target.Position + (tVel * predict)
			-- Fly-to-front : vole 4 studs devant la cible predite
			local forward = target.CFrame.LookVector
			local frontPos = predictedPos + forward * 4
			local dir = (frontPos - root.Position)
			if dir.Magnitude > 0.1 then
				dir = dir.Unit
				root.AssemblyLinearVelocity = Vector3.new(dir.X * 55, dir.Y * 55, dir.Z * 55)
			end
			-- frappe quand on est proche
			local dist = (target.Position - root.Position).Magnitude
			if dist <= 8 then tryHitBat() end
		else
			aimbotHighlight.Adornee = nil
		end
	end)
end

local function stopBatAimbot()
	batAimbotEnabled = false; State.autoBatToggled = false
	if aimbotConn then aimbotConn:Disconnect(); aimbotConn = nil end
	aimbotHighlight.Adornee = nil
	if proxy then pcall(function() proxy.AssemblyLinearVelocity = Vector3.new(0, proxy.AssemblyLinearVelocity.Y, 0) end) end
	local c = LP.Character
	local myHRP = c and c:FindFirstChild("HumanoidRootPart")
	local hum = c and c:FindFirstChildOfClass("Humanoid")
	if hum then hum.AutoRotate = true end
	if myHRP then
		myHRP.AssemblyAngularVelocity = Vector3.zero
		myHRP.AssemblyLinearVelocity = myHRP.AssemblyLinearVelocity * 0.3
	end
end

-- ===================================================================
-- GUI BUILDER
-- ===================================================================
for _,name in pairs({"JispiHubGUI","KINGzHubGUI"}) do
	local old = LP:FindFirstChild("PlayerGui") and LP.PlayerGui:FindFirstChild(name)
	if old then old:Destroy() end
end

local function addCorner(inst, r) local c=Instance.new("UICorner",inst); c.CornerRadius=UDim.new(0,r or 8); return c end
local function addStroke(inst, col, th, tr) local s=Instance.new("UIStroke",inst); s.Color=col; s.Thickness=th or 1; s.Transparency=tr or 0; return s end
local function addGradient(inst, c1, c2, rot)
	local g=Instance.new("UIGradient",inst)
	g.Color=ColorSequence.new(c1,c2); g.Rotation=rot or 0; return g
end

-- Pluie violette en arriere-plan (style Speed Bypass) - reutilisable
local function addRain(parent, corner)
	local clip = Instance.new("Frame", parent)
	clip.Name = DISC.name()
	clip.Size = UDim2.new(1,0,1,0)
	clip.BackgroundTransparency = 1
	clip.ClipsDescendants = true
	clip.ZIndex = (parent.ZIndex or 1)
	addCorner(clip, corner or 14)
	task.spawn(function()
		while clip and clip.Parent do
			local d = Instance.new("Frame", clip)
			d.BackgroundColor3 = Color3.fromRGB(140,0,10)
			d.BackgroundTransparency = math.random(15,40)/100
			d.BorderSizePixel = 0
			d.Size = UDim2.new(0,math.random(2,3),0,math.random(14,26))
			d.Position = UDim2.new(math.random(0,100)/100,0,0,-26)
			d.ZIndex = clip.ZIndex
			addCorner(d, 2)
			addGradient(d, Color3.fromRGB(180,20,30), Color3.fromRGB(90,0,5), 90)
			local dur = math.random(7,15)/10
			local t = TweenService:Create(d, TweenInfo.new(dur, Enum.EasingStyle.Linear), {Position = UDim2.new(d.Position.X.Scale,0,1,20)})
			t:Play(); t.Completed:Connect(function() d:Destroy() end)
			task.wait(0.05)
		end
	end)
	return clip
end

-- ECLIPSE DE SANG (full code, zero asset) : ciel noir + halo rouge qui pulse + soleil noir
-- A poser AVANT addRain pour rester en fond. ZIndex 0.
local function addEclipse(parent, corner)
	local z = parent.ZIndex or 1
	-- 1) Ciel : fond noir avec un degrade vers rouge tres sombre (bas)
	local sky = Instance.new("Frame", parent)
	sky.Name = DISC.name(); sky.Size = UDim2.new(1,0,1,0); sky.Position = UDim2.new(0,0,0,0)
	sky.BackgroundColor3 = Color3.fromRGB(8,2,4); sky.BorderSizePixel = 0; sky.ZIndex = z
	sky.ClipsDescendants = true; addCorner(sky, corner or 14)
	local skyGrad = Instance.new("UIGradient", sky)
	skyGrad.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0, Color3.fromRGB(2,0,1)),
		ColorSequenceKeypoint.new(0.55, Color3.fromRGB(20,2,6)),
		ColorSequenceKeypoint.new(1, Color3.fromRGB(45,4,10)),
	})
	skyGrad.Rotation = 90
	-- 2) Halo rouge incandescent (cercle large, degrade rouge -> transparent) qui pulse
	local halo = Instance.new("Frame", sky)
	halo.Name = DISC.name(); halo.AnchorPoint = Vector2.new(0.5,0.5)
	halo.Position = UDim2.new(0.5,0,0.42,0)
	halo.Size = UDim2.new(0,150,0,150); halo.BackgroundColor3 = Color3.fromRGB(190,15,25)
	halo.BorderSizePixel = 0; halo.ZIndex = z
	Instance.new("UICorner", halo).CornerRadius = UDim.new(1,0)
	local haloGrad = Instance.new("UIGradient", halo)
	haloGrad.Color = ColorSequence.new(Color3.fromRGB(255,40,30), Color3.fromRGB(120,0,8))
	haloGrad.Transparency = NumberSequence.new({
		NumberSequenceKeypoint.new(0, 0.25),
		NumberSequenceKeypoint.new(0.5, 0.55),
		NumberSequenceKeypoint.new(1, 1),
	})
	haloGrad.Rotation = 90
	-- 3) Soleil noir (disque sombre au centre du halo) avec fin liseré rouge
	local sun = Instance.new("Frame", sky)
	sun.Name = DISC.name(); sun.AnchorPoint = Vector2.new(0.5,0.5)
	sun.Position = UDim2.new(0.5,0,0.42,0)
	sun.Size = UDim2.new(0,74,0,74); sun.BackgroundColor3 = Color3.fromRGB(4,1,2)
	sun.BorderSizePixel = 0; sun.ZIndex = z
	Instance.new("UICorner", sun).CornerRadius = UDim.new(1,0)
	local sunStk = Instance.new("UIStroke", sun)
	sunStk.Color = Color3.fromRGB(255,60,40); sunStk.Thickness = 1.5; sunStk.Transparency = 0.2
	-- Pulse : le halo respire (grossit/retrecit + varie l'eclat du lisere)
	task.spawn(function()
		while halo and halo.Parent do
			TweenService:Create(halo, TweenInfo.new(1.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size = UDim2.new(0,182,0,182)}):Play()
			TweenService:Create(sunStk, TweenInfo.new(1.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.0}):Play()
			task.wait(1.8)
			TweenService:Create(halo, TweenInfo.new(1.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size = UDim2.new(0,150,0,150)}):Play()
			TweenService:Create(sunStk, TweenInfo.new(1.8, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency = 0.4}):Play()
			task.wait(1.8)
		end
	end)
	return sky
end

-- Slider reutilisable (style Ice Hub) : glisser pour regler une valeur
-- parent, yPos, label, minV, maxV, startV, suffix, zbase, onChange
local function makeSlider(parent, yPos, labelTxt, minV, maxV, startV, suffix, zbase, onChange)
	local z = zbase or 1
	local lbl = Instance.new("TextLabel", parent)
	lbl.Size = UDim2.new(1,-20,0,12); lbl.Position = UDim2.new(0,12,0,yPos)
	lbl.BackgroundTransparency = 1; lbl.TextColor3 = C_DIM
	lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 10; lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.ZIndex = z+1
	local function fmt(v) return labelTxt..": "..tostring(math.floor(v))..(suffix or "") end
	lbl.Text = fmt(startV)
	local track = Instance.new("Frame", parent)
	track.Size = UDim2.new(1,-24,0,6); track.Position = UDim2.new(0,12,0,yPos+16)
	track.BackgroundColor3 = Color3.fromRGB(36,18,20); track.BorderSizePixel = 0; track.ZIndex = z+1
	addCorner(track, 3)
	local fill = Instance.new("Frame", track)
	local startPct = (startV-minV)/(maxV-minV)
	fill.Size = UDim2.new(startPct,0,1,0); fill.BackgroundColor3 = C_YSLEM; fill.BorderSizePixel = 0; fill.ZIndex = z+2
	addCorner(fill, 3); addGradient(fill, C_YSLEM, C_PINK, 0)
	local knob = Instance.new("Frame", track)
	knob.Size = UDim2.new(0,14,0,14); knob.Position = UDim2.new(startPct,-7,0.5,-7)
	knob.BackgroundColor3 = C_WHITE; knob.BorderSizePixel = 0; knob.ZIndex = z+3
	addCorner(knob, 7)
	local hit = Instance.new("TextButton", track)
	hit.Size = UDim2.new(1,0,3,12); hit.Position = UDim2.new(0,0,-1,-6); hit.BackgroundTransparency = 1
	hit.Text = ""; hit.ZIndex = z+4; hit.AutoButtonColor = false
	local function setFromX(px)
		local rel = math.clamp((px - track.AbsolutePosition.X)/track.AbsoluteSize.X, 0, 1)
		local val = minV + rel*(maxV-minV)
		fill.Size = UDim2.new(rel,0,1,0)
		knob.Position = UDim2.new(rel,-7,0.5,-7)
		lbl.Text = fmt(val)
		if onChange then onChange(val) end
	end
	local dragging = false
	hit.InputBegan:Connect(function(i)
		if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
			dragging = true; setFromX(i.Position.X)
		end
	end)
	UIS.InputChanged:Connect(function(i)
		if dragging and (i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch) then
			setFromX(i.Position.X)
		end
	end)
	UIS.InputEnded:Connect(function(i)
		if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then dragging = false end
	end)
end

local gui = Instance.new("ScreenGui")
gui.Name = DISC.name(); gui.ResetOnSpawn = false; gui.DisplayOrder = 10
gui.IgnoreGuiInset = true; DISC.hide(gui)

-- STEAL BAR (restyle eclipse : header + barre fine + glow)
local stealProgressBar = Instance.new("Frame", gui)
stealProgressBar.Size = UDim2.new(0,250,0,84)
stealProgressBar.Position = UDim2.new(0.5,-125,1,-96)
stealProgressBar.BackgroundColor3 = C_BG
stealProgressBar.BorderSizePixel = 0; stealProgressBar.ZIndex = 100
stealProgressBar.ClipsDescendants = true
addCorner(stealProgressBar,14)
addStroke(stealProgressBar, C_YSLEM, 1.5, 0.3)
addEclipse(stealProgressBar, 14)
addRain(stealProgressBar, 14)

-- Header sombre (style widget)
local sbHd = Instance.new("Frame", stealProgressBar); sbHd.Size = UDim2.new(1,0,0,30); sbHd.BackgroundColor3 = C_HEADER; sbHd.BorderSizePixel = 0; sbHd.ZIndex = 101; addCorner(sbHd, 14)
do local sbAcc = Instance.new("Frame", stealProgressBar); sbAcc.Size = UDim2.new(1,0,0,2); sbAcc.Position = UDim2.new(0,0,0,30); sbAcc.BackgroundColor3 = C_YSLEM; sbAcc.BorderSizePixel = 0; sbAcc.ZIndex = 102; addGradient(sbAcc, C_YSLEM, C_PINK, 0) end
do local sbDot = Instance.new("Frame", sbHd); sbDot.Size = UDim2.new(0,6,0,6); sbDot.Position = UDim2.new(0,12,0.5,-3); sbDot.BackgroundColor3 = C_YSLEM; sbDot.BorderSizePixel = 0; sbDot.ZIndex = 103; addCorner(sbDot, 3); addGradient(sbDot, C_YSLEM, C_PINK, 45) end
local sbTitle = Instance.new("TextLabel", sbHd)
sbTitle.Size = UDim2.new(0,120,1,0); sbTitle.Position = UDim2.new(0,24,0,0)
sbTitle.BackgroundTransparency = 1; sbTitle.Text = "AUTO STEAL"
sbTitle.TextColor3 = C_WHITE; sbTitle.Font = Enum.Font.GothamBlack; sbTitle.TextSize = 12
sbTitle.TextXAlignment = Enum.TextXAlignment.Left; sbTitle.ZIndex = 103

-- % (dans le header, a droite avant le bouton minimize)
local pctLbl = Instance.new("TextLabel", sbHd)
pctLbl.Size = UDim2.new(0,48,1,0); pctLbl.Position = UDim2.new(1,-78,0,0)
pctLbl.BackgroundTransparency = 1; pctLbl.Text = "0%"
pctLbl.TextColor3 = C_ACCENT2; pctLbl.Font = Enum.Font.GothamBlack; pctLbl.TextSize = 14
pctLbl.TextXAlignment = Enum.TextXAlignment.Right; pctLbl.ZIndex = 103

-- Inputs RAD + DUR (cote a cote, sous le header)
local function makeBarInput(labelTxt, val, xScale, onChange)
	local lbl = Instance.new("TextLabel", stealProgressBar)
	lbl.Size = UDim2.new(0,30,0,16); lbl.Position = UDim2.new(xScale,0,0,38)
	lbl.BackgroundTransparency = 1; lbl.Text = labelTxt
	lbl.TextColor3 = C_DIM; lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 10
	lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.ZIndex = 102
	local box = Instance.new("TextBox", stealProgressBar)
	box.Size = UDim2.new(0,46,0,18); box.Position = UDim2.new(xScale,32,0,37)
	box.BackgroundColor3 = C_OFF_BG; box.BorderSizePixel = 0
	box.Text = tostring(val); box.TextColor3 = C_ACCENT
	box.Font = Enum.Font.GothamBlack; box.TextSize = 11
	box.ClearTextOnFocus = false; box.ZIndex = 103
	addCorner(box, 5); addStroke(box, C_BORDER, 1)
	box.FocusLost:Connect(function()
		local n = tonumber(box.Text)
		if n then onChange(n); box.Text = tostring(n)
		else box.Text = tostring(val) end
	end)
	return box, lbl
end

local barRadBox = makeBarInput("RAD", AutoSteal.Radius, 0.06, function(n)
	if n >= 1 and n <= 300 then
		AutoSteal.Radius = math.floor(n)
		if UIR.progressRadLbl then UIR.progressRadLbl.Text = tostring(AutoSteal.Radius) end
		_promptCache = nil
	end
end)
local barDurBox = makeBarInput("DUR", AutoSteal.Duration, 0.40, function(n)
	if n >= 0.05 and n <= 10 then AutoSteal.Duration = n end
end)
UIR.progressRadLbl = barRadBox

-- Toggle ON/OFF (a droite des inputs)
local barToggleOn = false
local barPill = Instance.new("Frame", stealProgressBar)
barPill.Size=UDim2.new(0,34,0,18); barPill.Position=UDim2.new(1,-46,0,37)
barPill.BackgroundColor3=C_OFF_BG; barPill.BorderSizePixel=0; barPill.ZIndex=103
addCorner(barPill,9)
local barPillStroke=Instance.new("UIStroke",barPill); barPillStroke.Color=Color3.fromRGB(80,28,30); barPillStroke.Thickness=1
local barDot=Instance.new("Frame",barPill)
barDot.Size=UDim2.new(0,12,0,12); barDot.Position=UDim2.new(0,3,0.5,-6)
barDot.BackgroundColor3=C_DIM; barDot.BorderSizePixel=0; barDot.ZIndex=104
addCorner(barDot,6)
local barOnLbl=Instance.new("TextLabel",stealProgressBar)
barOnLbl.Size=UDim2.new(0,30,0,16); barOnLbl.Position=UDim2.new(1,-84,0,38)
barOnLbl.BackgroundTransparency=1; barOnLbl.Text="OFF"
barOnLbl.TextColor3=C_DIM; barOnLbl.Font=Enum.Font.GothamBlack; barOnLbl.TextSize=10; barOnLbl.ZIndex=103
local barClk=Instance.new("TextButton",stealProgressBar)
barClk.Size=UDim2.new(0,44,0,22); barClk.Position=UDim2.new(1,-50,0,35)
barClk.BackgroundTransparency=1; barClk.Text=""; barClk.ZIndex=105; barClk.AutoButtonColor=false

-- Barre de progression (en bas)
local barTrack = Instance.new("Frame", stealProgressBar)
barTrack.Size = UDim2.new(1,-24,0,8); barTrack.Position = UDim2.new(0,12,1,-16)
barTrack.BackgroundColor3 = C_OFF_BG; barTrack.BorderSizePixel = 0; barTrack.ZIndex = 101
addCorner(barTrack,4)
local barFill = Instance.new("Frame", barTrack)
barFill.Size = UDim2.new(0,0,1,0); barFill.BackgroundColor3 = C_YSLEM
barFill.BorderSizePixel = 0; barFill.ZIndex = 102
addCorner(barFill,4)
addGradient(barFill, C_YSLEM, C_PINK, 0)

AutoSteal.ProgressFill = barFill
AutoSteal.ProgressText = pctLbl

local function setBarToggle(on)
	barToggleOn=on; AutoSteal.Enabled=on
	TweenService:Create(barPill,TweenInfo.new(0.2),{BackgroundColor3=on and C_ON_BG or C_OFF_BG}):Play()
	TweenService:Create(barPillStroke,TweenInfo.new(0.2),{Color=on and C_YSLEM or Color3.fromRGB(80,28,30)}):Play()
	TweenService:Create(barDot,TweenInfo.new(0.2,Enum.EasingStyle.Back),{
		Position=on and UDim2.new(1,-15,0.5,-6) or UDim2.new(0,3,0.5,-6),
		BackgroundColor3=on and C_WHITE or C_DIM
	}):Play()
	barOnLbl.Text=on and "ON" or "OFF"; barOnLbl.TextColor3=on and C_YSLEM or C_DIM
	if on then startAutoSteal() else stopAutoSteal() end
	if UIR.setInstaGrab then UIR.setInstaGrab(on) end
end
barClk.Activated:Connect(function() setBarToggle(not barToggleOn) end)

local _barSyncLast=0
RunService.Heartbeat:Connect(function()
	local _bsn=tick(); if _bsn-_barSyncLast<0.1 then return end; _barSyncLast=_bsn
	if barToggleOn ~= AutoSteal.Enabled then
		barToggleOn = AutoSteal.Enabled
		local on = barToggleOn
		barPill.BackgroundColor3 = on and C_ON_BG or C_OFF_BG
		barDot.Position = on and UDim2.new(1,-15,0.5,-6) or UDim2.new(0,3,0.5,-6)
		barDot.BackgroundColor3 = on and C_WHITE or C_DIM
		barOnLbl.Text = on and "ON" or "OFF"; barOnLbl.TextColor3 = on and C_YSLEM or C_DIM
		barPillStroke.Color = on and C_YSLEM or Color3.fromRGB(80,28,30)
	end
end)

-- Bouton minimize "-" dans le header : replie, ne laisse que header + barre
do
	local FULL_H = 84
	local sbCollapsed = false
	local toHide = {barRadBox, barDurBox, barPill, barOnLbl, barClk}
	local sbMinBtn = Instance.new("TextButton", sbHd)
	sbMinBtn.Size = UDim2.new(0,20,0,20); sbMinBtn.Position = UDim2.new(1,-26,0.5,-10)
	sbMinBtn.BackgroundColor3 = Color3.fromRGB(45,18,20); sbMinBtn.BorderSizePixel = 0
	sbMinBtn.Text = "-"; sbMinBtn.TextColor3 = C_WHITE; sbMinBtn.Font = Enum.Font.GothamBlack
	sbMinBtn.TextSize = 16; sbMinBtn.AutoButtonColor = false; sbMinBtn.ZIndex = 110
	addCorner(sbMinBtn, 6); addStroke(sbMinBtn, C_YSLEM, 1, 0.3)
	sbMinBtn.MouseButton1Click:Connect(function()
		sbCollapsed = not sbCollapsed
		sbMinBtn.Text = sbCollapsed and "+" or "-"
		for _, el in ipairs(toHide) do if el then el.Visible = not sbCollapsed end end
		for _, ch in ipairs(stealProgressBar:GetChildren()) do
			if ch:IsA("TextLabel") then ch.Visible = not sbCollapsed end
		end
		if sbCollapsed then
			stealProgressBar.Size = UDim2.new(0,250,0,46)
			barTrack.Position = UDim2.new(0,12,0,34)
		else
			stealProgressBar.Size = UDim2.new(0,250,0,FULL_H)
			barTrack.Position = UDim2.new(0,12,1,-16)
		end
	end)
end

-- ===================================================================
-- FOV WIDGET (flottant, +/- en jeu)
-- ===================================================================
UIR.fovWidget = Instance.new("Frame", gui)
UIR.fovWidget.Size = UDim2.new(0,164,0,36)
UIR.fovWidget.Position = UDim2.new(0.5,-82,0,70)
UIR.fovWidget.BackgroundColor3 = C_PANEL; UIR.fovWidget.BorderSizePixel = 0
UIR.fovWidget.ZIndex = 90; UIR.fovWidget.Visible = false
addCorner(UIR.fovWidget,10); addStroke(UIR.fovWidget, C_YSLEM, 1.5, 0.3)

local _fdn,_fds,_fsp=false,nil,nil
UIR.fovWidget.InputBegan:Connect(function(i)
	if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
		_fdn=true;_fds=i.Position;_fsp=UIR.fovWidget.Position
		i.Changed:Connect(function() if i.UserInputState==Enum.UserInputState.End then _fdn=false end end)
	end
end)
UIS.InputChanged:Connect(function(i)
	if not _fdn then return end
	if i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch then
		UIR.fovWidget.Position=UDim2.new(_fsp.X.Scale,_fsp.X.Offset+(i.Position.X-_fds.X),_fsp.Y.Scale,_fsp.Y.Offset+(i.Position.Y-_fds.Y))
	end
end)

local fovLabelW = Instance.new("TextLabel", UIR.fovWidget)
fovLabelW.Size=UDim2.new(0,32,1,0); fovLabelW.Position=UDim2.new(0,8,0,0)
fovLabelW.BackgroundTransparency=1; fovLabelW.Text="FOV"
fovLabelW.TextColor3=C_YSLEM; fovLabelW.Font=Enum.Font.GothamBlack; fovLabelW.TextSize=11; fovLabelW.ZIndex=92

local fovMinus = Instance.new("TextButton", UIR.fovWidget)
fovMinus.Size=UDim2.new(0,28,0,26); fovMinus.Position=UDim2.new(0,40,0.5,-13)
fovMinus.BackgroundColor3=C_ROW; fovMinus.BorderSizePixel=0
fovMinus.Text="-"; fovMinus.TextColor3=C_WHITE; fovMinus.Font=Enum.Font.GothamBlack
fovMinus.TextSize=16; fovMinus.ZIndex=92; fovMinus.AutoButtonColor=false
addCorner(fovMinus,6)

UIR.fovValLbl = Instance.new("TextLabel", UIR.fovWidget)
UIR.fovValLbl.Size=UDim2.new(0,40,1,0); UIR.fovValLbl.Position=UDim2.new(0,70,0,0)
UIR.fovValLbl.BackgroundTransparency=1; UIR.fovValLbl.Text=tostring(fovValue)
UIR.fovValLbl.TextColor3=C_WHITE; UIR.fovValLbl.Font=Enum.Font.GothamBlack; UIR.fovValLbl.TextSize=14; UIR.fovValLbl.ZIndex=92

local fovPlus = Instance.new("TextButton", UIR.fovWidget)
fovPlus.Size=UDim2.new(0,28,0,26); fovPlus.Position=UDim2.new(0,112,0.5,-13)
fovPlus.BackgroundColor3=C_ROW; fovPlus.BorderSizePixel=0
fovPlus.Text="+"; fovPlus.TextColor3=C_WHITE; fovPlus.Font=Enum.Font.GothamBlack
fovPlus.TextSize=14; fovPlus.ZIndex=92; fovPlus.AutoButtonColor=false
addCorner(fovPlus,6)

fovMinus.Activated:Connect(function()
	fovValue = math.max(30, fovValue - 5)
	UIR.fovValLbl.Text = tostring(fovValue)
	if fovEnabled then enableFOV() end
end)
fovPlus.Activated:Connect(function()
	fovValue = math.min(140, fovValue + 5)
	UIR.fovValLbl.Text = tostring(fovValue)
	if fovEnabled then enableFOV() end
end)



-- Bouton "-" en haut a droite d'un widget : replie/deplie (garde juste le header)
UIR.addMinimize = function(w, headerH)
	local fullH = w.Size.Y.Offset
	local collapsed = false
	local btn = Instance.new("TextButton", w)
	btn.Size = UDim2.new(0,22,0,22); btn.Position = UDim2.new(1,-26,0,6)
	btn.BackgroundColor3 = Color3.fromRGB(45,18,20); btn.BorderSizePixel = 0
	btn.Text = "-"; btn.TextColor3 = C_WHITE; btn.Font = Enum.Font.GothamBlack
	btn.TextSize = 16; btn.AutoButtonColor = false; btn.ZIndex = (w.ZIndex or 1)+25
	addCorner(btn, 6); addStroke(btn, C_YSLEM, 1, 0.3)
	btn.MouseButton1Click:Connect(function()
		collapsed = not collapsed
		w.Size = UDim2.new(w.Size.X.Scale, w.Size.X.Offset, 0, collapsed and headerH or fullH)
		btn.Text = collapsed and "+" or "-"
	end)
	return btn
end
-- Keybinds des widgets (clavier ou manette). Stockage dans UIR.WidgetBinds.
UIR.WidgetBinds = {}
UIR.addWidgetKeybind = function(parent, xPos, yPos, zbase, callback)
	local id = #UIR.WidgetBinds + 1
	UIR.WidgetBinds[id] = {key=Enum.KeyCode.Unknown, cb=callback}
	local btn = Instance.new("TextButton", parent)
	btn.Size = UDim2.new(0,40,0,18); btn.Position = UDim2.new(0,xPos,0,yPos)
	btn.BackgroundColor3 = Color3.fromRGB(0,0,0); btn.BorderSizePixel = 0
	btn.Text = "[...]"; btn.TextColor3 = Color3.fromRGB(200,200,210)
	btn.Font = Enum.Font.GothamBold; btn.TextSize = 10; btn.AutoButtonColor = false
	btn.ZIndex = (zbase or 1)+5
	addCorner(btn, 4); local bs = addStroke(btn, Color3.fromRGB(80,28,30), 1)
	local listening, lConn = false, nil
	local function stop(k)
		listening = false; _anyKeyListening = false
		if lConn then lConn:Disconnect(); lConn = nil end
		bs.Color = Color3.fromRGB(80,28,30); btn.TextColor3 = Color3.fromRGB(200,200,210)
		if k then UIR.WidgetBinds[id].key = k; btn.Text = k.Name:gsub("ButtonR","R"):gsub("ButtonL","L"):gsub("Button","") end
	end
	btn.Activated:Connect(function()
		if listening then stop(nil); return end
		listening = true; _anyKeyListening = true; btn.Text = "..."; btn.TextColor3 = C_WHITE; bs.Color = C_YSLEM
		lConn = UIS.InputBegan:Connect(function(inp)
			if not listening then return end
			if inp.UserInputType == Enum.UserInputType.Keyboard or inp.UserInputType.Name:match("^Gamepad") then
				if inp.KeyCode == Enum.KeyCode.Escape then stop(nil); return end
				stop(inp.KeyCode)
			end
		end)
	end)
	return btn
end



-- ===================================================================
-- SPEED BYPASS WIDGET
-- ===================================================================
do
	local w = Instance.new("Frame", gui)
	w.Name = DISC.name(); w.Size = UDim2.new(0,220,0,138); w.Position = UDim2.new(0.62,0,0,240)
	w.BackgroundColor3 = C_BG; w.BorderSizePixel = 0; w.ZIndex = 220; w.Visible = false
	w.ClipsDescendants = true
	addCorner(w, 12); addStroke(w, C_YSLEM, 1.5, 0.3)
	addEclipse(w, 12)  -- eclipse de sang Hades
	addRain(w, 12)
	local dn, ds, sp = false, nil, nil
	w.InputBegan:Connect(function(i)
		if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
			dn = true; ds = i.Position; sp = w.Position
			i.Changed:Connect(function() if i.UserInputState == Enum.UserInputState.End then dn = false end end)
		end
	end)
	UIS.InputChanged:Connect(function(i)
		if not dn then return end
		if i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch then
			local d = i.Position - ds
			w.Position = UDim2.new(sp.X.Scale, sp.X.Offset+d.X, sp.Y.Scale, sp.Y.Offset+d.Y)
		end
	end)
	-- Header (style Ice Hub : Yslem Hub / speed bypass)
	local hd = Instance.new("Frame", w); hd.Size = UDim2.new(1,0,0,40); hd.BackgroundColor3 = C_HEADER; hd.BorderSizePixel = 0; hd.ZIndex = 221; addCorner(hd, 12)
	local ab = Instance.new("Frame", w); ab.Size = UDim2.new(1,0,0,2); ab.Position = UDim2.new(0,0,0,40); ab.BackgroundColor3 = C_YSLEM; ab.BorderSizePixel = 0; ab.ZIndex = 222; addGradient(ab, C_YSLEM, C_PINK, 0)
	local dot = Instance.new("Frame", hd); dot.Size = UDim2.new(0,6,0,6); dot.Position = UDim2.new(0,10,0,10); dot.BackgroundColor3 = C_YSLEM; dot.BorderSizePixel = 0; dot.ZIndex = 223; addCorner(dot, 3); addGradient(dot, C_YSLEM, C_PINK, 45)
	local tt = Instance.new("TextLabel", hd); tt.Size = UDim2.new(1,-24,0,16); tt.Position = UDim2.new(0,22,0,4); tt.BackgroundTransparency = 1; tt.Text = "YSLEM HUB"; tt.TextColor3 = C_WHITE; tt.Font = Enum.Font.GothamBlack; tt.TextSize = 13; tt.TextXAlignment = Enum.TextXAlignment.Left; tt.ZIndex = 223
	local stt = Instance.new("TextLabel", hd); stt.Size = UDim2.new(1,-24,0,10); stt.Position = UDim2.new(0,22,0,23); stt.BackgroundTransparency = 1; stt.Text = "speed bypass"; stt.TextColor3 = C_DIM; stt.Font = Enum.Font.GothamMedium; stt.TextSize = 9; stt.TextXAlignment = Enum.TextXAlignment.Left; stt.ZIndex = 223
	-- Fonction de ligne toggle (Lag / Speed) avec keybind [...]
	local function makeIceRow(yPos, labelTxt, getState, onToggle, onKb)
		local row = Instance.new("Frame", w); row.Size = UDim2.new(1,-20,0,34); row.Position = UDim2.new(0,10,0,yPos); row.BackgroundColor3 = C_ROW; row.BorderSizePixel = 0; row.ZIndex = 222; addCorner(row, 8); addStroke(row, C_BORDER, 1)
		local lbl = Instance.new("TextLabel", row); lbl.Size = UDim2.new(1,-100,1,0); lbl.Position = UDim2.new(0,10,0,0); lbl.BackgroundTransparency = 1; lbl.Text = labelTxt; lbl.Font = Enum.Font.GothamBlack; lbl.TextSize = 13; lbl.TextColor3 = C_WHITE; lbl.TextXAlignment = Enum.TextXAlignment.Left; lbl.ZIndex = 223
		local kbBtn = Instance.new("TextButton", row); kbBtn.Size = UDim2.new(0,35,0,18); kbBtn.Position = UDim2.new(1,-86,0.5,-9); kbBtn.BackgroundColor3 = C_OFF_BG; kbBtn.Text = "[...]"; kbBtn.TextColor3 = C_WHITE; kbBtn.Font = Enum.Font.GothamBold; kbBtn.TextSize = 10; kbBtn.BorderSizePixel = 0; kbBtn.AutoButtonColor = false; kbBtn.ZIndex = 223; addCorner(kbBtn, 4)
		local track = Instance.new("Frame", row); track.Size = UDim2.new(0,36,0,18); track.Position = UDim2.new(1,-46,0.5,-9); track.BackgroundColor3 = Color3.fromRGB(22,22,28); track.BorderSizePixel = 0; track.ZIndex = 223; addCorner(track, 9); local trackStk = addStroke(track, Color3.fromRGB(80,28,30), 1)
		local knob = Instance.new("Frame", track); knob.Size = UDim2.new(0,14,0,14); knob.Position = UDim2.new(0,2,0.5,-7); knob.BackgroundColor3 = Color3.fromRGB(110,80,80); knob.BorderSizePixel = 0; knob.ZIndex = 224; addCorner(knob, 7)
		local btn = Instance.new("TextButton", row); btn.Size = UDim2.new(0,46,1,0); btn.Position = UDim2.new(1,-46,0,0); btn.BackgroundTransparency = 1; btn.Text = ""; btn.ZIndex = 225; btn.AutoButtonColor = false
		local function refresh()
			local on = getState()
			TweenService:Create(knob, TweenInfo.new(0.15), {Position = on and UDim2.new(1,-16,0.5,-7) or UDim2.new(0,2,0.5,-7), BackgroundColor3 = on and C_WHITE or Color3.fromRGB(110,80,80)}):Play()
			TweenService:Create(track, TweenInfo.new(0.15), {BackgroundColor3 = on and Color3.fromRGB(120,25,28) or Color3.fromRGB(22,22,28)}):Play()
			TweenService:Create(trackStk, TweenInfo.new(0.15), {Color = on and C_YSLEM or Color3.fromRGB(80,28,30)}):Play()
		end
		btn.MouseButton1Click:Connect(function() onToggle(); refresh() end)
		kbBtn.MouseButton1Click:Connect(function() onKb(kbBtn) end)
		return refresh
	end
	-- Toggle Lag (self-freeze Ice Hub)
	makeIceRow(48, "Lag", function() return SB.lagOn end, function()
		SB.lagOn = not SB.lagOn
		if SB.lagOn then SB.startLag() else SB.stopLag() end
	end, function(kbBtn) kbBtn.Text = "[...]" end)
	-- Slider Lag Power (% comme Ice Hub : 0-150%)
	makeSlider(w, 96, "Lag Power", 0, 150, math.floor(SB.lagAmount/0.2*150), "%", 222, function(v)
		SB.lagAmount = (v/150)*0.2
	end)
	UIR.addWidgetKeybind(w, 152, 8, w.ZIndex, function()
		SB.lagOn = not SB.lagOn
		if SB.lagOn then SB.startLag() else SB.stopLag() end
	end)
	UIR.addMinimize(w, 40)
	SB.widget = w
end

-- ===================================================================
-- ANTI BAT WIDGET
-- ===================================================================
do
	local w = Instance.new("Frame", gui)
	w.Name = DISC.name(); w.Size = UDim2.new(0,220,0,224); w.Position = UDim2.new(0.30,0,0,260)
	w.BackgroundColor3 = C_BG; w.BorderSizePixel = 0; w.ZIndex = 210; w.Visible = false
	w.ClipsDescendants = true
	addCorner(w, 12); addStroke(w, C_YSLEM, 1.5, 0.3)
	addEclipse(w, 12)  -- eclipse de sang Hades
	addRain(w, 12)
	local dn, ds, sp = false, nil, nil
	w.InputBegan:Connect(function(i)
		if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
			dn = true; ds = i.Position; sp = w.Position
			i.Changed:Connect(function() if i.UserInputState == Enum.UserInputState.End then dn = false end end)
		end
	end)
	UIS.InputChanged:Connect(function(i)
		if not dn then return end
		if i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch then
			local d = i.Position - ds
			w.Position = UDim2.new(sp.X.Scale, sp.X.Offset+d.X, sp.Y.Scale, sp.Y.Offset+d.Y)
		end
	end)
	local hd = Instance.new("Frame", w); hd.Size = UDim2.new(1,0,0,34); hd.BackgroundColor3 = C_HEADER; hd.BorderSizePixel = 0; hd.ZIndex = 211; addCorner(hd, 12)
	local ac = Instance.new("Frame", w); ac.Size = UDim2.new(1,0,0,2); ac.Position = UDim2.new(0,0,0,34); ac.BackgroundColor3 = C_YSLEM; ac.BorderSizePixel = 0; ac.ZIndex = 212; addGradient(ac, C_YSLEM, C_PINK, 0)
	local dot = Instance.new("Frame", hd); dot.Size = UDim2.new(0,6,0,6); dot.Position = UDim2.new(0,10,0.5,-3); dot.BackgroundColor3 = C_YSLEM; dot.BorderSizePixel = 0; dot.ZIndex = 213; addCorner(dot, 3); addGradient(dot, C_YSLEM, C_PINK, 45)
	local tt = Instance.new("TextLabel", hd); tt.Size = UDim2.new(1,-24,0,14); tt.Position = UDim2.new(0,22,0,4); tt.BackgroundTransparency = 1; tt.Text = "ANTI BAT"; tt.TextColor3 = C_WHITE; tt.Font = Enum.Font.GothamBlack; tt.TextSize = 11; tt.TextXAlignment = Enum.TextXAlignment.Left; tt.ZIndex = 213
	local stt = Instance.new("TextLabel", hd); stt.Size = UDim2.new(1,-24,0,10); stt.Position = UDim2.new(0,22,0,20); stt.BackgroundTransparency = 1; stt.Text = "Fling Defense"; stt.TextColor3 = C_YSLEM; stt.Font = Enum.Font.GothamBold; stt.TextSize = 8; stt.TextXAlignment = Enum.TextXAlignment.Left; stt.ZIndex = 213
	local rw = Instance.new("Frame", w); rw.Size = UDim2.new(1,-20,0,40); rw.Position = UDim2.new(0,10,0,42); rw.BackgroundColor3 = C_ROW; rw.BorderSizePixel = 0; rw.ZIndex = 212; addCorner(rw, 8); addStroke(rw, C_BORDER, 1)
	local plbl = Instance.new("TextLabel", rw); plbl.Size = UDim2.new(0.5,0,0,16); plbl.Position = UDim2.new(0,12,0,4); plbl.BackgroundTransparency = 1; plbl.Text = "PROTECTION"; plbl.TextColor3 = C_ACCENT; plbl.Font = Enum.Font.GothamBold; plbl.TextSize = 11; plbl.TextXAlignment = Enum.TextXAlignment.Left; plbl.ZIndex = 213
	AB.statusLbl = Instance.new("TextLabel", rw); AB.statusLbl.Size = UDim2.new(0.5,0,0,11); AB.statusLbl.Position = UDim2.new(0,12,0,23); AB.statusLbl.BackgroundTransparency = 1; AB.statusLbl.Text = "DISABLED"; AB.statusLbl.TextColor3 = Color3.fromRGB(150,95,95); AB.statusLbl.Font = Enum.Font.GothamBold; AB.statusLbl.TextSize = 9; AB.statusLbl.TextXAlignment = Enum.TextXAlignment.Left; AB.statusLbl.ZIndex = 213
	AB.pill = Instance.new("Frame", rw); AB.pill.Size = UDim2.new(0,34,0,18); AB.pill.Position = UDim2.new(1,-44,0.5,-9); AB.pill.BackgroundColor3 = Color3.fromRGB(22,22,28); AB.pill.ZIndex = 213; addCorner(AB.pill, 9); AB.pillStk = addStroke(AB.pill, Color3.fromRGB(80,28,30), 1)
	AB.ball = Instance.new("Frame", AB.pill); AB.ball.Size = UDim2.new(0,10,0,10); AB.ball.Position = UDim2.new(0,4,0.5,-5); AB.ball.BackgroundColor3 = Color3.fromRGB(110,80,80); AB.ball.ZIndex = 214; addCorner(AB.ball, 5)
	local ck = Instance.new("TextButton", rw); ck.Size = UDim2.new(1,0,1,0); ck.BackgroundTransparency = 1; ck.Text = ""; ck.ZIndex = 215; ck.AutoButtonColor = false; ck.MouseButton1Click:Connect(AB.toggle)
	local rwj = Instance.new("Frame", w); rwj.Size = UDim2.new(1,-20,0,40); rwj.Position = UDim2.new(0,10,0,86); rwj.BackgroundColor3 = C_ROW; rwj.BorderSizePixel = 0; rwj.ZIndex = 212; addCorner(rwj, 8); addStroke(rwj, C_BORDER, 1)
	local jlbl = Instance.new("TextLabel", rwj); jlbl.Size = UDim2.new(0.5,0,0,16); jlbl.Position = UDim2.new(0,12,0,4); jlbl.BackgroundTransparency = 1; jlbl.Text = "INF JUMP"; jlbl.TextColor3 = C_ACCENT; jlbl.Font = Enum.Font.GothamBold; jlbl.TextSize = 11; jlbl.TextXAlignment = Enum.TextXAlignment.Left; jlbl.ZIndex = 213
	IJ.statusLbl = Instance.new("TextLabel", rwj); IJ.statusLbl.Size = UDim2.new(0.5,0,0,11); IJ.statusLbl.Position = UDim2.new(0,12,0,23); IJ.statusLbl.BackgroundTransparency = 1; IJ.statusLbl.Text = "DISABLED"; IJ.statusLbl.TextColor3 = Color3.fromRGB(150,95,95); IJ.statusLbl.Font = Enum.Font.GothamBold; IJ.statusLbl.TextSize = 9; IJ.statusLbl.TextXAlignment = Enum.TextXAlignment.Left; IJ.statusLbl.ZIndex = 213
	IJ.pill = Instance.new("Frame", rwj); IJ.pill.Size = UDim2.new(0,34,0,18); IJ.pill.Position = UDim2.new(1,-44,0.5,-9); IJ.pill.BackgroundColor3 = Color3.fromRGB(22,22,28); IJ.pill.ZIndex = 213; addCorner(IJ.pill, 9); IJ.pillStk = addStroke(IJ.pill, Color3.fromRGB(80,28,30), 1)
	IJ.ball = Instance.new("Frame", IJ.pill); IJ.ball.Size = UDim2.new(0,10,0,10); IJ.ball.Position = UDim2.new(0,4,0.5,-5); IJ.ball.BackgroundColor3 = Color3.fromRGB(110,80,80); IJ.ball.ZIndex = 214; addCorner(IJ.ball, 5)
	local ckj = Instance.new("TextButton", rwj); ckj.Size = UDim2.new(1,0,1,0); ckj.BackgroundTransparency = 1; ckj.Text = ""; ckj.ZIndex = 215; ckj.AutoButtonColor = false; ckj.MouseButton1Click:Connect(function()
		IJ.active = not IJ.active
		if IJ.active then IJ.start() else IJ.stop() end
		IJ.refresh()
		if IJ.active then
			if State.infJumpEnabled then State.infJumpEnabled=false; if UIR.setInfJumpOld then UIR.setInfJumpOld(false) end end
			if UIR.setInfJumpNew then UIR.setInfJumpNew(false) end
		end
	end)
	local info = Instance.new("TextLabel", w); info.Size = UDim2.new(1,-20,0,24); info.Position = UDim2.new(0,10,0,130); info.BackgroundTransparency = 1; info.Text = "Enable Anti Bat first, then Infinite Jump"; info.TextColor3 = Color3.fromRGB(150,140,180); info.Font = Enum.Font.GothamBold; info.TextSize = 8; info.TextWrapped = true; info.TextXAlignment = Enum.TextXAlignment.Center; info.ZIndex = 213
	local dr = Instance.new("Frame", w); dr.Size = UDim2.new(1,-20,0,20); dr.Position = UDim2.new(0,10,0,190); dr.BackgroundColor3 = Color3.fromRGB(55,14,18); dr.BorderSizePixel = 0; dr.ZIndex = 212; addCorner(dr, 6); addStroke(dr, C_YSLEM, 1, 0.3)
	local dl = Instance.new("TextLabel", dr); dl.Size = UDim2.new(1,0,1,0); dl.BackgroundTransparency = 1; dl.Text = DISCORD_LINK; dl.TextColor3 = C_ACCENT; dl.Font = Enum.Font.GothamBold; dl.TextSize = 10; dl.ZIndex = 213
	if UIR.addWidgetKeybind then UIR.addWidgetKeybind(w, 124, 8, w.ZIndex, AB.toggle) end
	if UIR.addWidgetKeybind then UIR.addWidgetKeybind(w, 166, 8, w.ZIndex, function()
		IJ.active = not IJ.active
		if IJ.active then IJ.start() else IJ.stop() end
		IJ.refresh()
	end) end
	if UIR.addMinimize then UIR.addMinimize(w, 34) end
	AB.widget = w
end


-- MAIN PANEL
local main = Instance.new("Frame", gui)
main.Name = DISC.name(); main.Size = UDim2.new(0,260,0,370)
main.Position = UDim2.new(0,20,0,20)
main.BackgroundColor3 = C_BG; main.BorderSizePixel = 0; main.Active = true; main.ClipsDescendants = true
addCorner(main,16)
addStroke(main, C_YSLEM, 1.5, 0.3)
addEclipse(main, 16)  -- eclipse de sang Hades en fond
addRain(main, 16)  -- pluie de sang en fond

local function makeDraggable(frame, handle)
	handle = handle or frame
	local dragging=false; local dragStart=nil; local startPos=nil
	handle.InputBegan:Connect(function(inp)
		if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
			dragging=true; dragStart=inp.Position; startPos=frame.Position
			inp.Changed:Connect(function() if inp.UserInputState==Enum.UserInputState.End then dragging=false end end)
		end
	end)
	UIS.InputChanged:Connect(function(inp)
		if not dragging then return end
		if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then
			local d = inp.Position - dragStart
			frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset+d.X, startPos.Y.Scale, startPos.Y.Offset+d.Y)
		end
	end)
end
-- Drag steal bar conditionnel (respecte le verrou)
local sbDragging, sbDragStart, sbStartPos = false, nil, nil
stealProgressBar.InputBegan:Connect(function(inp)
	if MobileButtons.Locked then return end
	if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then
		sbDragging = true; sbDragStart = inp.Position; sbStartPos = stealProgressBar.Position
		inp.Changed:Connect(function() if inp.UserInputState == Enum.UserInputState.End then sbDragging = false end end)
	end
end)
UIS.InputChanged:Connect(function(inp)
	if not sbDragging then return end
	if inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch then
		local d = inp.Position - sbDragStart
		stealProgressBar.Position = UDim2.new(sbStartPos.X.Scale, sbStartPos.X.Offset+d.X, sbStartPos.Y.Scale, sbStartPos.Y.Offset+d.Y)
	end
end)

-- HEADER
local header = Instance.new("Frame", main)
header.Size = UDim2.new(1,0,0,52); header.BackgroundColor3 = C_HEADER; header.BorderSizePixel = 0; header.ZIndex = 5
header.Active = true
addCorner(header,16)
makeDraggable(main, header)

local accentBar = Instance.new("Frame", main)
accentBar.Size = UDim2.new(1,0,0,2); accentBar.Position = UDim2.new(0,0,0,52)
accentBar.BackgroundColor3 = C_YSLEM; accentBar.BorderSizePixel = 0; accentBar.ZIndex = 6
addGradient(accentBar, C_YSLEM, C_PINK, 0)

local logoDot = Instance.new("Frame", header)
logoDot.Size = UDim2.new(0,8,0,8); logoDot.Position = UDim2.new(0,14,0.5,-4)
logoDot.BackgroundColor3 = C_YSLEM; logoDot.BorderSizePixel = 0; logoDot.ZIndex = 7
addCorner(logoDot,4); addGradient(logoDot, C_YSLEM, C_PINK, 45)

local titleLbl = Instance.new("TextLabel", header)
titleLbl.Size = UDim2.new(1,-80,0,20); titleLbl.Position = UDim2.new(0,28,0,10)
titleLbl.BackgroundTransparency = 1; titleLbl.Text = "YSLEM HUB"
titleLbl.TextColor3 = C_WHITE; titleLbl.Font = Enum.Font.GothamBlack; titleLbl.TextSize = 15
titleLbl.TextXAlignment = Enum.TextXAlignment.Left; titleLbl.ZIndex = 7

local subLbl = Instance.new("TextLabel", header)
subLbl.Size = UDim2.new(1,-80,0,12); subLbl.Position = UDim2.new(0,28,0,30)
subLbl.BackgroundTransparency = 1; subLbl.Text = DISCORD_LINK
subLbl.TextColor3 = C_YSLEM; subLbl.Font = Enum.Font.GothamBold; subLbl.TextSize = 9
subLbl.TextXAlignment = Enum.TextXAlignment.Left; subLbl.ZIndex = 7

local closeBtn = Instance.new("TextButton", gui)
closeBtn.Size = UDim2.new(0,26,0,26)
closeBtn.BackgroundColor3 = Color3.fromRGB(35,35,40); closeBtn.BorderSizePixel = 0
closeBtn.Text = "X"; closeBtn.TextColor3 = Color3.fromRGB(160,160,170)
closeBtn.Font = Enum.Font.GothamBlack; closeBtn.TextSize = 12; closeBtn.ZIndex = 50
addCorner(closeBtn,13)

local miniBtn = Instance.new("TextButton", gui)
miniBtn.Name = DISC.name(); miniBtn.Size = UDim2.new(0,52,0,52)
miniBtn.Position = UDim2.new(0,8,0,66)
miniBtn.BackgroundColor3 = Color3.fromRGB(6,3,4); miniBtn.BorderSizePixel = 0
miniBtn.Text = ""; miniBtn.TextColor3 = C_WHITE
miniBtn.AutoButtonColor = false
miniBtn.Visible = false; miniBtn.ZIndex = 50
do local c=Instance.new("UICorner",miniBtn); c.CornerRadius=UDim.new(1,0) end  -- cercle
-- Halo rouge diffus (eclipse) derriere le bouton
local mbHalo = Instance.new("Frame", miniBtn)
mbHalo.Name = DISC.name(); mbHalo.AnchorPoint = Vector2.new(0.5,0.5)
mbHalo.Position = UDim2.new(0.5,0,0.5,0); mbHalo.Size = UDim2.new(1.35,0,1.35,0)
mbHalo.BackgroundColor3 = Color3.fromRGB(190,20,28); mbHalo.BorderSizePixel = 0; mbHalo.ZIndex = 49
do local c=Instance.new("UICorner",mbHalo); c.CornerRadius=UDim.new(1,0) end
do local g=Instance.new("UIGradient",mbHalo); g.Color=ColorSequence.new(Color3.fromRGB(255,50,40),Color3.fromRGB(90,0,8))
	g.Transparency=NumberSequence.new({NumberSequenceKeypoint.new(0,0.35),NumberSequenceKeypoint.new(1,1)}); g.Rotation=90 end
-- Disque noir (soleil noir) avec anneau rouge incandescent
local mb_stroke = Instance.new("UIStroke",miniBtn); mb_stroke.Color=Color3.fromRGB(255,55,45); mb_stroke.Thickness=2.5; mb_stroke.Transparency=0.1
local mbInner = Instance.new("Frame", miniBtn)
mbInner.Name = DISC.name(); mbInner.AnchorPoint = Vector2.new(0.5,0.5)
mbInner.Position = UDim2.new(0.5,0,0.5,0); mbInner.Size = UDim2.new(1,-6,1,-6)
mbInner.BackgroundColor3 = Color3.fromRGB(8,3,4); mbInner.BorderSizePixel = 0; mbInner.ZIndex = 51
do local c=Instance.new("UICorner",mbInner); c.CornerRadius=UDim.new(1,0) end
do local g=Instance.new("UIGradient",mbInner); g.Color=ColorSequence.new(Color3.fromRGB(30,4,8),Color3.fromRGB(6,2,3)); g.Rotation=90 end
-- Lettre Y rouge eclatante
local mbY = Instance.new("TextLabel", mbInner)
mbY.Size=UDim2.new(1,0,1,0); mbY.BackgroundTransparency=1
mbY.Text="Y"; mbY.TextColor3=Color3.fromRGB(255,75,60); mbY.Font=Enum.Font.GothamBlack; mbY.TextSize=26; mbY.ZIndex=52
mbY.TextStrokeColor3=Color3.fromRGB(120,0,8); mbY.TextStrokeTransparency=0.3
-- Pulse de l'anneau + halo (respire comme l'eclipse)
task.spawn(function()
	while miniBtn and miniBtn.Parent do
		TweenService:Create(mb_stroke, TweenInfo.new(1.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency=0.55}):Play()
		TweenService:Create(mbHalo, TweenInfo.new(1.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size=UDim2.new(1.55,0,1.55,0)}):Play()
		task.wait(1.6)
		TweenService:Create(mb_stroke, TweenInfo.new(1.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Transparency=0.1}):Play()
		TweenService:Create(mbHalo, TweenInfo.new(1.6, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut), {Size=UDim2.new(1.35,0,1.35,0)}):Play()
		task.wait(1.6)
	end
end)
makeDraggable(miniBtn)

closeBtn.MouseButton1Click:Connect(function() main.Visible=false; closeBtn.Visible=false; miniBtn.Visible=true end)
miniBtn.MouseButton1Click:Connect(function() main.Visible=true; closeBtn.Visible=true; miniBtn.Visible=false end)

local function updateCloseBtn()
	if main.Visible then
		local p = main.Position
		closeBtn.Position = UDim2.new(p.X.Scale, p.X.Offset+260-34, p.Y.Scale, p.Y.Offset+13)
	end
end
main:GetPropertyChangedSignal("Position"):Connect(updateCloseBtn)
main:GetPropertyChangedSignal("Visible"):Connect(updateCloseBtn)
updateCloseBtn()

-- SCROLL
local scroll = Instance.new("ScrollingFrame", main)
scroll.Size = UDim2.new(1,0,1,-54); scroll.Position = UDim2.new(0,0,0,54)
scroll.BackgroundTransparency = 1; scroll.BorderSizePixel = 0; scroll.ScrollBarThickness = 3
scroll.ScrollBarImageColor3 = C_YSLEM; scroll.AutomaticCanvasSize = Enum.AutomaticSize.Y
scroll.CanvasSize = UDim2.new(0,0,0,0); scroll.ZIndex = 2
local listLayout = Instance.new("UIListLayout", scroll)
listLayout.SortOrder = Enum.SortOrder.LayoutOrder; listLayout.Padding = UDim.new(0,6)
local pad = Instance.new("UIPadding", scroll)
pad.PaddingLeft=UDim.new(0,12); pad.PaddingRight=UDim.new(0,12); pad.PaddingTop=UDim.new(0,12); pad.PaddingBottom=UDim.new(0,14)

local lo = 0
local function LO() lo = lo+1; return lo end
local function makeGap(px) local f=Instance.new("Frame",scroll); f.Size=UDim2.new(1,0,0,px or 4); f.BackgroundTransparency=1; f.LayoutOrder=LO() end

local function makeSectionLabel(text)
	local row=Instance.new("Frame",scroll); row.Size=UDim2.new(1,0,0,22); row.BackgroundTransparency=1; row.LayoutOrder=LO()
	local lbl=Instance.new("TextLabel",row); lbl.Size=UDim2.new(1,0,1,0); lbl.BackgroundTransparency=1
	lbl.Text=text:upper(); lbl.TextColor3=C_YSLEM; lbl.Font=Enum.Font.GothamBlack; lbl.TextSize=10; lbl.TextXAlignment=Enum.TextXAlignment.Left
end

local function makeInputRow(label, default, onChange)
	local row = Instance.new("Frame", scroll); row.Size = UDim2.new(1,0,0,34); row.BackgroundColor3 = C_ROW
	row.BorderSizePixel = 0; row.LayoutOrder = LO()
	Instance.new("UICorner", row).CornerRadius = UDim.new(0,8)
	local lbl = Instance.new("TextLabel", row); lbl.Size = UDim2.new(0.55,0,1,0); lbl.Position = UDim2.new(0,12,0,0)
	lbl.BackgroundTransparency = 1; lbl.Text = label; lbl.TextColor3 = C_ACCENT
	lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 12; lbl.TextXAlignment = Enum.TextXAlignment.Left
	local box = Instance.new("TextBox", row); box.Size = UDim2.new(0,80,0,26); box.Position = UDim2.new(1,-86,0.5,-13)
	box.BackgroundColor3 = Color3.fromRGB(0,0,0); box.BorderSizePixel = 0
	box.Text = tostring(default); box.TextColor3 = C_ACCENT
	box.Font = Enum.Font.GothamBold; box.TextSize = 14; box.ClearTextOnFocus = false
	box.TextEditable = true; box.Active = true; box.Selectable = true; box.ZIndex = 8
	Instance.new("UICorner", box).CornerRadius = UDim.new(0,6)
	local bs = Instance.new("UIStroke", box); bs.Color = C_BORDER2; bs.Thickness = 1
	box.FocusLost:Connect(function()
		local num = tonumber(box.Text)
		if num then
			local f = math.clamp(num, 0.05, 9999)
			if f == math.floor(f) then box.Text = tostring(math.floor(f)) else box.Text = tostring(f) end
			if onChange then onChange(tostring(f)) end; autoSaveConfig()
		else box.Text = tostring(default) end
	end)
	return box
end

local function makeStatusRow(label, valTxt)
	local row=Instance.new("Frame",scroll); row.Size=UDim2.new(1,0,0,28); row.BackgroundColor3=C_ROW
	row.BorderSizePixel=0; row.LayoutOrder=LO()
	Instance.new("UICorner",row).CornerRadius=UDim.new(0,8)
	local lbl=Instance.new("TextLabel",row); lbl.Size=UDim2.new(0.5,0,1,0); lbl.Position=UDim2.new(0,12,0,0)
	lbl.BackgroundTransparency=1; lbl.Text=label; lbl.TextColor3=C_ACCENT; lbl.Font=Enum.Font.GothamBold
	lbl.TextSize=12; lbl.TextXAlignment=Enum.TextXAlignment.Left
	local val=Instance.new("TextLabel",row); val.Size=UDim2.new(0.45,-10,1,0); val.Position=UDim2.new(0.52,0,0,0)
	val.BackgroundTransparency=1; val.Text=valTxt; val.TextColor3=C_ACCENT2
	val.Font=Enum.Font.GothamBlack; val.TextSize=12; val.TextXAlignment=Enum.TextXAlignment.Right
	return val
end

-- Registre global des boutons keybind (pour mise à jour croisée)
local _keybindBtns = {}
local ALL_KEYBIND_KEYS = {"keyAutoLeft","keyAutoRight","keyDropBR","keyTpDown","keyAutoBat","keyCarrySpeed","keyTpUp","keyLaggerMode","keyInstaReset","keyLaggerUse"}

local function assignExclusiveKey(stateKey, kc)
	-- Retire ce keybind de toutes les autres touches
	for _, otherKey in ipairs(ALL_KEYBIND_KEYS) do
		if otherKey ~= stateKey and State[otherKey] == kc then
			State[otherKey] = Enum.KeyCode.Unknown
			if _keybindBtns[otherKey] then
				_keybindBtns[otherKey].Text = "-"
			end
		end
	end
	State[stateKey] = kc
end

local function makeToggleRow(label, defaultOn, onToggle, keybindStateKey)
	local row=Instance.new("Frame",scroll); row.Size=UDim2.new(1,0,0,30); row.BackgroundColor3=C_ROW; row.LayoutOrder=LO()
	Instance.new("UICorner",row).CornerRadius=UDim.new(0,8)
	local lblWidth = keybindStateKey and 0.5 or 0.7
	local lbl=Instance.new("TextLabel",row); lbl.Size=UDim2.new(lblWidth,-4,1,0); lbl.Position=UDim2.new(0,10,0,0)
	lbl.BackgroundTransparency=1; lbl.Text=label; lbl.TextColor3=C_ACCENT
	lbl.Font=Enum.Font.GothamBold; lbl.TextSize=11; lbl.TextXAlignment=Enum.TextXAlignment.Left

	if keybindStateKey then
		local currentKey = State[keybindStateKey]
		local keyName = (currentKey and currentKey ~= Enum.KeyCode.Unknown) and currentKey.Name or "-"
		local kbBtn = Instance.new("TextButton", row)
		kbBtn.Size = UDim2.new(0,56,0,20); kbBtn.Position = UDim2.new(1,-106,0.5,-10)
		kbBtn.BackgroundColor3 = Color3.fromRGB(0,0,0); kbBtn.BorderSizePixel = 0
		kbBtn.Text = keyName; kbBtn.TextColor3 = Color3.fromRGB(200,200,210)
		kbBtn.Font = Enum.Font.GothamBold; kbBtn.TextSize = 10; kbBtn.ZIndex = 6
		Instance.new("UICorner", kbBtn).CornerRadius = UDim.new(0,4)
		local kbs = Instance.new("UIStroke", kbBtn); kbs.Color = Color3.fromRGB(80,28,30); kbs.Thickness = 1
		_keybindBtns[keybindStateKey] = kbBtn  -- registre global
		local listening = false; local lConn = nil
		local function stopListen(k)
			listening = false; _anyKeyListening = false
			if lConn then lConn:Disconnect(); lConn=nil end
			kbs.Color = Color3.fromRGB(80,28,30); kbBtn.TextColor3 = Color3.fromRGB(200,200,210)
			if k then assignExclusiveKey(keybindStateKey, k); kbBtn.Text = k.Name; autoSaveConfig() end
		end
		kbBtn.Activated:Connect(function()
			if listening then stopListen(nil); return end
			listening = true; _anyKeyListening = true; kbBtn.Text = "..."; kbBtn.TextColor3 = C_WHITE; kbs.Color = C_YSLEM
			lConn = UIS.InputBegan:Connect(function(inp, gpe)
				if not listening then return end
				if inp.UserInputType == Enum.UserInputType.Keyboard or inp.UserInputType == Enum.UserInputType.Gamepad1 then
					if inp.KeyCode == Enum.KeyCode.Escape then stopListen(nil); return end
					stopListen(inp.KeyCode)
				end
			end)
		end)
	end

	local pill = Instance.new("Frame", row); pill.Size = UDim2.new(0,40,0,20); pill.Position = UDim2.new(1,-46,0.5,-10)
	pill.BackgroundColor3 = defaultOn and C_ON_BG or C_OFF_BG; pill.BorderSizePixel = 0; pill.ZIndex = 5
	Instance.new("UICorner",pill).CornerRadius=UDim.new(1,0)
	local pStroke = Instance.new("UIStroke",pill); pStroke.Color=defaultOn and C_YSLEM or C_BORDER; pStroke.Thickness=1
	local dot = Instance.new("Frame", pill); dot.Size = UDim2.new(0,14,0,14)
	dot.Position = defaultOn and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7)
	dot.BackgroundColor3 = defaultOn and C_WHITE or C_DIM; dot.BorderSizePixel = 0; dot.ZIndex = 6
	Instance.new("UICorner",dot).CornerRadius=UDim.new(1,0)
	local isOn = defaultOn or false
	local function setV(on)
		isOn = on
		TweenService:Create(pill,TweenInfo.new(0.2),{BackgroundColor3=on and C_ON_BG or C_OFF_BG}):Play()
		TweenService:Create(pStroke,TweenInfo.new(0.2),{Color=on and C_YSLEM or C_BORDER}):Play()
		TweenService:Create(dot,TweenInfo.new(0.2,Enum.EasingStyle.Back),{Position=on and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7), BackgroundColor3=on and C_WHITE or C_DIM}):Play()
	end
	local clk = Instance.new("TextButton", row); clk.Size = UDim2.new(1,0,1,0); clk.BackgroundTransparency = 1; clk.Text = ""; clk.ZIndex = 3
	clk.MouseButton1Click:Connect(function() isOn = not isOn; setV(isOn); if onToggle then pcall(onToggle,isOn) end; autoSaveConfig() end)
	return setV
end

-- Ligne keybind seule (pas de toggle) — évite le conflit avec le clk invisible
local function makeKeybindRow(label, stateKey)
	local row = Instance.new("Frame", scroll)
	row.Size = UDim2.new(1,0,0,28); row.BackgroundColor3 = C_ROW
	row.BorderSizePixel = 0; row.LayoutOrder = LO()
	Instance.new("UICorner", row).CornerRadius = UDim.new(0,8)
	local lbl = Instance.new("TextLabel", row)
	lbl.Size = UDim2.new(0.55,0,1,0); lbl.Position = UDim2.new(0,10,0,0)
	lbl.BackgroundTransparency = 1; lbl.Text = label; lbl.TextColor3 = C_ACCENT
	lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 11; lbl.TextXAlignment = Enum.TextXAlignment.Left
	local currentKey = State[stateKey]
	local keyName = (currentKey and currentKey ~= Enum.KeyCode.Unknown) and currentKey.Name or "-"
	local kbBtn = Instance.new("TextButton", row)
	kbBtn.Size = UDim2.new(0,80,0,20); kbBtn.Position = UDim2.new(1,-86,0.5,-10)
	kbBtn.BackgroundColor3 = Color3.fromRGB(0,0,0); kbBtn.BorderSizePixel = 0
	kbBtn.Text = keyName; kbBtn.TextColor3 = Color3.fromRGB(200,200,210)
	kbBtn.Font = Enum.Font.GothamBold; kbBtn.TextSize = 10; kbBtn.ZIndex = 4
	Instance.new("UICorner", kbBtn).CornerRadius = UDim.new(0,4)
	local kbs = Instance.new("UIStroke", kbBtn); kbs.Color = Color3.fromRGB(80,28,30); kbs.Thickness = 1
	_keybindBtns[stateKey] = kbBtn  -- registre global
	local listening = false; local lConn = nil
	local function stopListen(k)
		listening = false; _anyKeyListening = false
		if lConn then lConn:Disconnect(); lConn = nil end
		kbs.Color = Color3.fromRGB(80,28,30); kbBtn.TextColor3 = Color3.fromRGB(200,200,210)
		if k then assignExclusiveKey(stateKey, k); kbBtn.Text = k.Name; autoSaveConfig() end
	end
	kbBtn.Activated:Connect(function()
		if listening then stopListen(nil); return end
		listening = true; _anyKeyListening = true; kbBtn.Text = "..."; kbBtn.TextColor3 = C_WHITE; kbs.Color = C_YSLEM
		lConn = UIS.InputBegan:Connect(function(inp, gpe)
			if not listening then return end
			if inp.UserInputType == Enum.UserInputType.Keyboard or inp.UserInputType == Enum.UserInputType.Gamepad1 then
				if inp.KeyCode == Enum.KeyCode.Escape then stopListen(nil); return end
				stopListen(inp.KeyCode)
			end
		end)
	end)
end
-- ===================================================================
-- ===================================================================
-- BUILD UI
-- ===================================================================
makeSectionLabel("Speed")
UIR.normalBox = makeInputRow("Normal Speed", State.normalSpeed, function(v) local n=tonumber(v); if n and n>0 then State.normalSpeed=n end end)
UIR.carryBox = makeInputRow("Carry Speed", State.carrySpeed, function(v) local n=tonumber(v); if n and n>0 then State.carrySpeed=n end end)
UIR.laggerBox = makeInputRow("Lagger Normal", State.laggerSpeed, function(v) local n=tonumber(v); if n and n>0 then State.laggerSpeed=n end end)
UIR.carryLaggerBox = makeInputRow("Lagger Steal", State.laggerCarrySpeed, function(v) local n=tonumber(v); if n and n>0 then State.laggerCarrySpeed=n end end)
UIR.modeValLbl = makeStatusRow("Mode", "Normal")
local setCarryToggle
setCarryToggle = makeToggleRow("Carry Speed Toggle", State.speedType == "carry", function(on)
	State._carryManualUntil = tick() + 2.5
	if on then State.speedType = "carry" else State.speedType = "normal" end
	refreshUIToggles()
	if MobileButtons.Buttons and MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(on) end
	autoSaveConfig()
end, "keyCarrySpeed")
local setLaggerToggle
setLaggerToggle = makeToggleRow("Lagger Mode", State.laggerActive, function(on)
	if on then toggleLagger() else if State.laggerActive then toggleLagger() end end
	if MobileButtons.Buttons and MobileButtons.Buttons.lagger then MobileButtons.Buttons.lagger(State.laggerActive) end
	autoSaveConfig()
end, "keyLaggerMode")
UIR.laggerUseBox = makeInputRow("Lagger Use Speed", State.laggerUseAmount, function(v) local n=tonumber(v); if n and n>0 then State.laggerUseAmount=n end end)
UIR.setLaggerUse = makeToggleRow("Lagger Use", State.laggerUseEnabled, function(on)
	if on then
		if not State.laggerUseEnabled then toggleLaggerUse() end
	else
		if State.laggerUseEnabled then toggleLaggerUse() end
	end
	if MobileButtons.Buttons and MobileButtons.Buttons.laggerUse then MobileButtons.Buttons.laggerUse(State.laggerUseEnabled) end
	autoSaveConfig()
end, "keyLaggerUse")
makeToggleRow("Speed Bypass", false, function(on)
	if SB.widget then SB.widget.Visible = on end
	if not on then
		if SB.lagOn then SB.lagOn = false; SB.stopLag() end
	end
end)
makeToggleRow("Anti Bat (Fling)", false, function(on)
	if AB.widget then AB.widget.Visible = on end
	if not on and AB.active then AB.toggle() end
end)

makeGap(6)
makeSectionLabel("Combat")
UIR.setAutoBat = makeToggleRow("Auto Bat (Aimbot)", false, function(on)
	if on then startBatAimbot() else stopBatAimbot() end
	if MobileButtons.Buttons.autoBat then MobileButtons.Buttons.autoBat(on) end
end)
makeKeybindRow("  Touche Bat Aimbot", "keyAutoBat")
UIR.setBatCounter = makeToggleRow("Bat Counter", false, function(on)
	BC.active = on
	if on then BC.start() else BC.stop() end
end)

makeGap(6)
makeSectionLabel("Auto Steal")
local radiusBox = makeInputRow("Grab Radius", AutoSteal.Radius, function(v)
	local n = tonumber(v); if n and n>=5 and n<=300 then AutoSteal.Radius = math.floor(n); if UIR.progressRadLbl then UIR.progressRadLbl.Text = tostring(AutoSteal.Radius) end; _promptCache = nil end
end)
local durationBox = makeInputRow("Steal Duration", AutoSteal.Duration, function(v)
	local n = tonumber(v); if n and n>=0.05 and n<=10 then AutoSteal.Duration = n end
end)
UIR.setInstaGrab = makeToggleRow("Auto Grab", false, function(on)
	AutoSteal.Enabled = on
	if on then startAutoSteal() else stopAutoSteal() end
end)
UIR.setAutoCarry = makeToggleRow("Auto Carry on Grab", State.autoCarryOnGrab, function(on) State.autoCarryOnGrab=on end)

makeGap(6)
makeSectionLabel("Movement")
UIR.setInfJumpOld = makeToggleRow("Infinite Jump", false, function(on) State.infJumpEnabled=on end)
UIR.setAutoTpDown = makeToggleRow("Auto Tp Down", false, function(on) State.autoTpDownEnabled=on; autoSaveConfig() end)
makeInputRow("Y Trigger", State.autoTpDownY, function(v) local n=tonumber(v); if n then State.autoTpDownY=n; autoSaveConfig() end end)
UIR.setAntiRag = makeToggleRow("Anti Ragdoll", false, function(on) State.antiRagdollEnabled=on; if on then startAntiRagdoll() else stopAntiRagdoll() end end)
UIR.setFps = makeToggleRow("FPS Boost", true, function(on) State.fpsBoostEnabled=on; if on then pcall(applyFPSBoost) end end)
UIR.setMedusaCounter = makeToggleRow("Medusa Counter", false, function(on) State.medusaCounterEnabled=on; if LP.Character then setupMedusaCounter(LP.Character) end end)
UIR.setMedusaReset = makeToggleRow("Medusa Auto Reset", true, function(on) State.medusaAutoReset=on; if LP.Character then setupMedusaCounter(LP.Character) end end)
UIR.setHarderHit = makeToggleRow("Harder Hit Anim", false, function(on) HHA.enabled=on; if on then HHA.start() else HHA.stop() end end)
UIR.setAntiLag = makeToggleRow("Anti Lag", true, function(on) if on then ANL.start() else ANL.stop() end end)
UIR.setUnwalkToggle = makeToggleRow("Unwalk", false, function(on) if on then startUnwalk() else stopUnwalk() end end)

makeGap(6)
makeSectionLabel("Teleport")
local setMenuInstaReset
setMenuInstaReset = makeToggleRow("Insta Reset", false, function(on)
	if on then instaReset(); task.delay(0.5, function() if setMenuInstaReset then setMenuInstaReset(false) end end) end
end, "keyInstaReset")
UIR.setMenuDropBR = makeToggleRow("Drop Brainrot", false, function(on)
	if on then runDropBrainrot(); task.delay(0.5,function() if UIR.setMenuDropBR then UIR.setMenuDropBR(false) end; if MobileButtons.Buttons.dropBR then MobileButtons.Buttons.dropBR(false) end end) end
end, "keyDropBR")
UIR.setMenuTpDown = makeToggleRow("Tp Down", false, function(on)
	if on then tpToGround(); task.delay(0.5,function() if UIR.setMenuTpDown then UIR.setMenuTpDown(false) end; if MobileButtons.Buttons.tpDown then MobileButtons.Buttons.tpDown(false) end end) end
end, "keyTpDown")
local setMenuTpUp
setMenuTpUp = makeToggleRow("Tp Up", false, function(on)
	if on then tpUp(); task.delay(0.5,function() if setMenuTpUp then setMenuTpUp(false) end; if MobileButtons.Buttons.tpUp then MobileButtons.Buttons.tpUp(false) end end) end
end, "keyTpUp")
UIR.setAutoLeft = makeToggleRow("Auto Left", false, function(on)
	State.autoLeftEnabled = on
	if MobileButtons.Buttons.autoLeft then MobileButtons.Buttons.autoLeft(on) end
	if on then startAutoLeft(); if not AutoSteal.Enabled then AutoSteal.Enabled=true; startAutoSteal(); if UIR.setInstaGrab then UIR.setInstaGrab(true) end end
	else stopAutoLeft() end
end, "keyAutoLeft")
UIR.setAutoRight = makeToggleRow("Auto Right", false, function(on)
	State.autoRightEnabled = on
	if MobileButtons.Buttons.autoRight then MobileButtons.Buttons.autoRight(on) end
	if on then startAutoRight(); if not AutoSteal.Enabled then AutoSteal.Enabled=true; startAutoSteal(); if UIR.setInstaGrab then UIR.setInstaGrab(true) end end
	else stopAutoRight() end
end, "keyAutoRight")

makeGap(6)
makeSectionLabel("Visuals")
makeInputRow("FOV", fovValue, function(v)
	local n = tonumber(v); if n and n >= 30 and n <= 140 then
		fovValue = n
		if fovEnabled then enableFOV() end
	end
end)
makeToggleRow("FOV Modifier", false, function(on)
	fovEnabled = on; if on then enableFOV() else disableFOV() end
end)

makeGap(6)
makeSectionLabel("UI")
makeToggleRow("Lock Boutons Mobile", false, function(on)
	MobileButtons.Locked = on; autoSaveConfig()
end)
makeGap(4)
makeSectionLabel("Taunt")
local tauntActive = false
makeToggleRow("Spam Chat (yslem hub on top)", false, function(on)
	tauntActive = on
	if on then
		task.spawn(function()
			while tauntActive do
				pcall(function()
					local tcs = game:GetService("TextChatService")
					local ch = tcs and tcs.TextChannels and tcs.TextChannels:FindFirstChild("RBXGeneral")
					if ch then ch:SendAsync("yslem hub on top") end
				end)
				task.wait(2)
			end
		end)
	end
end)

makeGap(8)
local footerLbl = Instance.new("TextLabel", scroll)
footerLbl.Size = UDim2.new(1,0,0,14); footerLbl.BackgroundTransparency = 1; footerLbl.LayoutOrder = LO()
footerLbl.Text = "Yslem Hub v2.1"; footerLbl.TextColor3 = C_DIM
footerLbl.Font = Enum.Font.GothamBold; footerLbl.TextSize = 10; footerLbl.TextXAlignment = Enum.TextXAlignment.Center
local discordBtn = Instance.new("TextButton", scroll)
discordBtn.Size = UDim2.new(1,0,0,24); discordBtn.BackgroundColor3 = Color3.fromRGB(88,101,242)
discordBtn.BorderSizePixel = 0; discordBtn.LayoutOrder = LO(); discordBtn.ZIndex = 3
discordBtn.Text = "Copier Discord"; discordBtn.TextColor3 = Color3.fromRGB(255,255,255)
discordBtn.Font = Enum.Font.GothamBold; discordBtn.TextSize = 11
addCorner(discordBtn, 8)
discordBtn.Activated:Connect(function()
	setclipboard(DISCORD_LINK)
	discordBtn.Text = "Copie !"
	discordBtn.BackgroundColor3 = Color3.fromRGB(59,165,93)
	task.delay(2, function()
		discordBtn.Text = "Copier Discord"
		discordBtn.BackgroundColor3 = Color3.fromRGB(88,101,242)
	end)
end)


refreshUIToggles = function()
	if State.laggerUseEnabled then UIR.modeValLbl.Text = "Lagger Use"
	elseif State.laggerCarryActive then UIR.modeValLbl.Text = "Lagger Carry"
	elseif State.laggerActive then UIR.modeValLbl.Text = "Lagger Normal"
	else UIR.modeValLbl.Text = (State.speedType=="normal") and "Normal" or "Carry" end
	if setCarryToggle then setCarryToggle(State.speedType == "carry") end
end

-- ===================================================================
-- MOBILE BUTTONS PANEL (à droite, déplaçables)
-- ===================================================================
local function createMobilePanel()
	local panel = Instance.new("ScreenGui")
	panel.Name = DISC.name(); DISC.hide(panel)
	panel.ResetOnSpawn = false; panel.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

	local BTN_W,BTN_H,CORNER,GAP = 46,46,10,2
	local MARGIN = 4
	local COL2_X = -(BTN_W+MARGIN)
	local COL1_X = -(BTN_W+GAP+BTN_W+MARGIN)
	local BASE_Y = 0.28
	local NR = 6; local totalH = NR*BTN_H+(NR-1)*GAP
	local function RY(n) return -totalH/2+(n-1)*(BTN_H+GAP) end
	local ROW_Y = {RY(1),RY(2),RY(3),RY(4),RY(5),RY(6)}

	-- Conteneur pour tout cacher d'un coup (minimize)
	local mobileContainer = Instance.new("Frame", panel)
	mobileContainer.Size = UDim2.new(1,0,1,0)
	mobileContainer.BackgroundTransparency = 1
	mobileContainer.BorderSizePixel = 0
	mobileContainer.Visible = true

	-- Bouton minimize (-) fixe en haut à droite — plus gros et draggable
	local minified = false
	local minBtn = Instance.new("TextButton", panel)
	minBtn.Size = UDim2.new(0,36,0,36)
	minBtn.Position = UDim2.new(1,-(BTN_W+MARGIN+36+4),0.5,-totalH/2-44)
	minBtn.BackgroundColor3 = Color3.fromRGB(140,20,24)
	minBtn.BorderSizePixel = 0; minBtn.ZIndex = 10
	minBtn.Text = "-"; minBtn.TextColor3 = Color3.fromRGB(255,255,255)
	minBtn.Font = Enum.Font.GothamBlack; minBtn.TextSize = 22
	minBtn.AutoButtonColor = false
	do local c=Instance.new("UICorner",minBtn); c.CornerRadius=UDim.new(0,10) end
	do local g2=Instance.new("UIGradient",minBtn); g2.Color=ColorSequence.new(Color3.fromRGB(225,35,40),Color3.fromRGB(255,80,40)); g2.Rotation=135 end
	do local s2=Instance.new("UIStroke",minBtn); s2.Color=Color3.fromRGB(230,40,45); s2.Thickness=2 end
	-- Drag du bouton minimize (lui seul se déplace, pas le container)
	local _md,_mds,_msp=false,nil,nil
	minBtn.InputBegan:Connect(function(i)
		if i.UserInputType==Enum.UserInputType.MouseButton1 or i.UserInputType==Enum.UserInputType.Touch then
			_md=true;_mds=i.Position;_msp=minBtn.Position
			i.Changed:Connect(function() if i.UserInputState==Enum.UserInputState.End then _md=false end end)
		end
	end)
	UIS.InputChanged:Connect(function(i)
		if not _md then return end
		if i.UserInputType==Enum.UserInputType.MouseMovement or i.UserInputType==Enum.UserInputType.Touch then
			local d=i.Position-_mds
			minBtn.Position=UDim2.new(_msp.X.Scale,_msp.X.Offset+d.X,_msp.Y.Scale,_msp.Y.Offset+d.Y)
		end
	end)
	minBtn.Activated:Connect(function()
		minified = not minified
		mobileContainer.Visible = not minified
		minBtn.Text = minified and "+" or "-"
		minBtn.TextColor3 = minified and Color3.fromRGB(225,35,40) or Color3.fromRGB(200,200,210)
	end)

	local function makeBtn(name, text, defaultPos, saveKey, callback, isToggle)
		local cont = Instance.new("Frame", mobileContainer); cont.Name = name
		cont.BackgroundColor3 = C_PANEL; cont.BorderSizePixel = 0
		cont.Size = UDim2.new(0,BTN_W,0,BTN_H); cont.Position = defaultPos
		addCorner(cont,12); local cstroke = addStroke(cont, C_BORDER, 1.2, 0.2)
		-- degrade interne sombre (profondeur)
		do local g=Instance.new("UIGradient",cont); g.Color=ColorSequence.new(Color3.fromRGB(26,14,15),Color3.fromRGB(14,8,9)); g.Rotation=90 end
		-- petit accent lumineux en haut du bouton
		local accent = Instance.new("Frame", cont); accent.Size = UDim2.new(0.5,0,0,2); accent.Position = UDim2.new(0.25,0,0,3)
		accent.BackgroundColor3 = C_YSLEM; accent.BorderSizePixel = 0; accent.ZIndex = 2
		accent.BackgroundTransparency = 0.4; addCorner(accent, 2); addGradient(accent, C_YSLEM, C_PINK, 0)
		local btn = Instance.new("TextButton", cont); btn.Size = UDim2.new(1,0,1,0)
		btn.BackgroundTransparency = 1; btn.BorderSizePixel = 0
		btn.Font = Enum.Font.GothamBlack; btn.Text = text
		btn.TextColor3 = C_ACCENT; btn.TextSize = 12; btn.TextWrapped = true; btn.AutoButtonColor = false; btn.ZIndex = 3
		local active = false
		local function setActive(s)
			active = s
			TweenService:Create(cont,TweenInfo.new(0.18),{BackgroundColor3 = s and C_ON_BG or C_PANEL}):Play()
			TweenService:Create(cstroke,TweenInfo.new(0.18),{Color = s and C_YSLEM or C_BORDER, Thickness = s and 1.8 or 1.2, Transparency = s and 0 or 0.2}):Play()
			TweenService:Create(accent,TweenInfo.new(0.18),{BackgroundTransparency = s and 0 or 0.4}):Play()
			btn.TextColor3 = s and C_WHITE or C_ACCENT
		end
		btn.MouseButton1Click:Connect(function()
			if isToggle then setActive(not active) end
			if callback then callback(setActive, active) end
		end)
		local dragging,dragStart,startPos = false,nil,nil
		cont.InputBegan:Connect(function(inp)
			if MobileButtons.Locked then return end
			if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
				dragging=true; dragStart=inp.Position; startPos=cont.Position
				inp.Changed:Connect(function() if inp.UserInputState==Enum.UserInputState.End then dragging=false end end)
			end
		end)
		UIS.InputChanged:Connect(function(inp)
			if MobileButtons.Locked then dragging=false; return end
			if not dragging then return end
			if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then
				local d = inp.Position - dragStart
				cont.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset+d.X, startPos.Y.Scale, startPos.Y.Offset+d.Y)
			end
		end)
		local saved = nil
		pcall(function() saved = readfile(saveKey) end)
		if saved and saved ~= "" then
			local parts = {}
			for p in string.gmatch(saved,"[^,]+") do table.insert(parts,p) end
			if #parts >= 4 then cont.Position = UDim2.new(tonumber(parts[1]),tonumber(parts[2]),tonumber(parts[3]),tonumber(parts[4])) end
		end
		local lastSave = 0
		cont:GetPropertyChangedSignal("Position"):Connect(function()
			if tick()-lastSave > 0.5 then
				lastSave = tick()
				local p = cont.Position
				local s = string.format("%.3f,%.1f,%.3f,%.1f",p.X.Scale,p.X.Offset,p.Y.Scale,p.Y.Offset)
				pcall(function() writefile(saveKey,s) end)
			end
		end)
		cont.Visible = true
		table.insert(MobileButtons.Containers, cont)
		return setActive
	end

	local dropBR = makeBtn("BtnDropBR","DROP\nBR",UDim2.new(1,COL1_X,BASE_Y,ROW_Y[1]),"rbxdata_b05.txt",function(setA) runDropBrainrot(); if UIR.setMenuDropBR then UIR.setMenuDropBR(true) end; task.delay(0.5,function() if UIR.setMenuDropBR then UIR.setMenuDropBR(false) end; setA(false) end) end, true)
	local autoLeft = makeBtn("BtnAutoLeft","AUTO\nLEFT",UDim2.new(1,COL2_X,BASE_Y,ROW_Y[1]),"rbxdata_b02.txt",function(setA)
		State.autoLeftEnabled = not State.autoLeftEnabled
		if UIR.setAutoLeft then UIR.setAutoLeft(State.autoLeftEnabled) end
		if State.autoLeftEnabled then
			startAutoLeft(); setA(true)
			if not AutoSteal.Enabled then AutoSteal.Enabled=true; startAutoSteal(); if UIR.setInstaGrab then UIR.setInstaGrab(true) end end
		else stopAutoLeft(); setA(false) end
		autoSaveConfig()
	end, true)
	local autoBat = makeBtn("BtnAutoBat","BAT\nAIMBOT",UDim2.new(1,COL1_X,BASE_Y,ROW_Y[2]),"rbxdata_b01.txt",function(setA) if not batAimbotEnabled then startBatAimbot(); setA(true) else stopBatAimbot(); setA(false) end; if UIR.setAutoBat then UIR.setAutoBat(batAimbotEnabled) end; autoSaveConfig() end, true)
	local autoRight = makeBtn("BtnAutoRight","AUTO\nRIGHT",UDim2.new(1,COL2_X,BASE_Y,ROW_Y[2]),"rbxdata_b03.txt",function(setA)
		State.autoRightEnabled = not State.autoRightEnabled
		if UIR.setAutoRight then UIR.setAutoRight(State.autoRightEnabled) end
		if State.autoRightEnabled then
			startAutoRight(); setA(true)
			if not AutoSteal.Enabled then AutoSteal.Enabled=true; startAutoSteal(); if UIR.setInstaGrab then UIR.setInstaGrab(true) end end
		else stopAutoRight(); setA(false) end
		autoSaveConfig()
	end, true)
	local tpDown = makeBtn("BtnTpDown","TP\nDOWN",UDim2.new(1,COL1_X,BASE_Y,ROW_Y[3]),"rbxdata_b09.txt",function(setA) tpToGround(); if UIR.setMenuTpDown then UIR.setMenuTpDown(true) end; task.delay(0.5,function() if UIR.setMenuTpDown then UIR.setMenuTpDown(false) end; setA(false) end) end, true)
	local carrySpd = makeBtn("BtnCarrySpd","CARRY\nSPD",UDim2.new(1,COL2_X,BASE_Y,ROW_Y[3]),"rbxdata_b04.txt",function(setA) toggleSpeedType(); setA(State.speedType=="carry") end, true)
	local lagger = makeBtn("BtnLagger","LAGGER\nMODE",UDim2.new(1,COL1_X,BASE_Y,ROW_Y[4]),"rbxdata_b07.txt",function(setA) toggleLagger(); setA(State.laggerActive) end, true)
	local tpUpBtn = makeBtn("BtnTpUp","TP\nUP",UDim2.new(1,COL2_X,BASE_Y,ROW_Y[4]),"rbxdata_b10.txt",function(setA) tpUp(); if setMenuTpUp then setMenuTpUp(true) end; task.delay(0.5,function() if setMenuTpUp then setMenuTpUp(false) end; setA(false) end) end, true)
	local instaResetBtn = makeBtn("BtnInstaReset","INSTA\nRESET",UDim2.new(1,COL1_X,BASE_Y,ROW_Y[5]),"rbxdata_b06.txt",function(setA) instaReset(); setA(true); task.delay(0.5,function() setA(false) end) end, true)
	local laggerUseBtn = makeBtn("BtnLaggerUse","LAGGER\nUSE",UDim2.new(1,COL2_X,BASE_Y,ROW_Y[5]),"rbxdata_b08.txt",function(setA) toggleLaggerUse(); setA(State.laggerUseEnabled); if UIR.setLaggerUse then UIR.setLaggerUse(State.laggerUseEnabled) end end, true)

	MobileButtons.Buttons = {autoLeft=autoLeft,autoRight=autoRight,autoBat=autoBat,carrySpeed=carrySpd,lagger=lagger,dropBR=dropBR,tpDown=tpDown,tpUp=tpUpBtn,instaReset=instaResetBtn,laggerUse=laggerUseBtn}
	task.spawn(function()
		task.wait(0.1)
		if MobileButtons.Buttons.autoLeft then MobileButtons.Buttons.autoLeft(State.autoLeftEnabled) end
		if MobileButtons.Buttons.autoRight then MobileButtons.Buttons.autoRight(State.autoRightEnabled) end
		if MobileButtons.Buttons.autoBat then MobileButtons.Buttons.autoBat(State.autoBatToggled) end
		if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(State.speedType=="carry") end
		if MobileButtons.Buttons.lagger then MobileButtons.Buttons.lagger(State.laggerActive) end
		if MobileButtons.Buttons.laggerCarry then MobileButtons.Buttons.laggerCarry(State.laggerCarryActive) end
		if MobileButtons.Buttons.laggerUse then MobileButtons.Buttons.laggerUse(State.laggerUseEnabled) end
	end)
	return panel
end

task.spawn(function() task.wait(0.5); createMobilePanel() end)

-- ===================================================================
-- CHARACTER SETUP
-- ===================================================================
local h, hrp, speedLbl
local function setupChar(char)
	task.wait(0.1)
	h = char:WaitForChild("Humanoid",5); hrp = char:WaitForChild("HumanoidRootPart",5)
	if not h or not hrp then return end
	local head = char:FindFirstChild("Head")
	if head then
		local oldBB = head:FindFirstChild("_bbInfo"); if oldBB then oldBB:Destroy() end
		local bb = Instance.new("BillboardGui", head)
		bb.Name = "_bbInfo"; bb.Size = UDim2.new(0,140,0,25)
		bb.StudsOffset = Vector3.new(0,3,0); bb.AlwaysOnTop = true
		speedLbl = Instance.new("TextLabel", bb); speedLbl.Size = UDim2.new(1,0,1,0)
		speedLbl.BackgroundTransparency = 1; speedLbl.TextColor3 = C_ACCENT2
		speedLbl.Font = Enum.Font.GothamBold; speedLbl.TextScaled = true; speedLbl.TextStrokeTransparency = 0
	end
	if State.antiRagdollEnabled and not Conns.antiRag then task.wait(0.5); startAntiRagdoll() end
	stopMedusaCounter()
	-- Detection medusa TOUJOURS active (pour l'auto insta reset quand on est meduse)
	setupMedusaCounter(char)
	if HHA.enabled then task.wait(0.3); HHA.start() end
	if State.unwalkEnabled then State.unwalkEnabled=false; task.wait(0.3); startUnwalk() end
	watchCharacterTools(char)
	setupFullDebug(char, h, hrp)
	-- Bug fix : arrêter l'aimbot et reset le patrol au respawn
	if batAimbotEnabled then stopBatAimbot() end
	patrolFrozen = false; patrolFreezeUntil = 0
	-- Recréer le proxy
	proxy = nil; ensureProxy()
	task.spawn(function()
		task.wait(0.5)
		if State.autoLeftEnabled then stopAutoLeft(); State.autoLeftEnabled = true; startAutoLeft()
		elseif State.autoRightEnabled then stopAutoRight(); State.autoRightEnabled = true; startAutoRight() end
		-- Fix : relancer l'auto grab si il était actif avant le respawn
		if AutoSteal.Enabled then
			stopAutoSteal()
			task.wait(0.1)
			startAutoSteal()
			if UIR.setInstaGrab then UIR.setInstaGrab(true) end
		end
	end)
	_promptCache = nil
	_plotIsMyCache = {}
end

LP.CharacterAdded:Connect(setupChar)
if LP.Character then task.spawn(function() setupChar(LP.Character) end) end

-- ===================================================================
-- NO COLLIDE (event-driven)
-- ===================================================================
local function setupNoCollideForChar(char)
	for _,p in ipairs(char:GetDescendants()) do if p:IsA("BasePart") then p.CanCollide = false end end
	char.DescendantAdded:Connect(function(p) if p:IsA("BasePart") then p.CanCollide = false end end)
end
Players.PlayerAdded:Connect(function(p)
	if p == LP then return end
	p.CharacterAdded:Connect(setupNoCollideForChar)
	if p.Character then setupNoCollideForChar(p.Character) end
end)
for _,p in ipairs(Players:GetPlayers()) do
	if p ~= LP and p.Character then setupNoCollideForChar(p.Character) end
end

-- ===================================================================
-- MAIN LOOPS
-- ===================================================================
UIS.JumpRequest:Connect(function()
	if not State.infJumpEnabled then return end
	if State.infJumpMode ~= "manual" then return end
	local char = LP.Character; if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart")
	if root then root.Velocity = Vector3.new(root.Velocity.X, 55, root.Velocity.Z) end
end)

RunService.Heartbeat:Connect(function()
	if not State.infJumpEnabled and not State.autoTpDownEnabled then return end
	local char = LP.Character; if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
	if State.infJumpEnabled then
		local hum2 = char:FindFirstChildOfClass("Humanoid")
		if State.infJumpMode == "hold" then
			local held = UIS:IsKeyDown(Enum.KeyCode.Space) or (hum2 and hum2.Jump == true)
			if held and root.Velocity.Y < 30 then
				root.Velocity = Vector3.new(root.Velocity.X, 55, root.Velocity.Z)
			end
		end
		if root.Velocity.Y < -120 then root.Velocity = Vector3.new(root.Velocity.X, -120, root.Velocity.Z) end
	end
	if State.autoTpDownEnabled and root.Position.Y >= State.autoTpDownY then
		local rot = root.CFrame.Rotation
		root.CFrame = CFrame.new(root.Position.X, -8.80, root.Position.Z) * rot
	end
end)

RunService.Stepped:Connect(function()
	if not (h and hrp) then return end
	if State._tpInProgress then return end
	-- Aimbot gère son propre mouvement via proxyMove — skip le loop normal
	if batAimbotEnabled then return end
	if State.autoLeftEnabled or State.autoRightEnabled then
		if speedLbl then
			local p = ensureProxy()
			local vel = p and p.AssemblyLinearVelocity or Vector3.zero
			speedLbl.Text = "Speed: "..string.format("%.1f", Vector3.new(vel.X,0,vel.Z).Magnitude)
		end
		return
	end
	local md = h.MoveDirection
	local spd = getCurrentSpeed()
	if md.Magnitude > 0 then
		State.lastMoveDir = md
		proxyMove(md, spd)
	elseif State.antiRagdollEnabled and State.lastMoveDir.Magnitude > 0 then
		local anyHeld = false
		for key in pairs(MOVE_KEYS) do if UIS:IsKeyDown(key) then anyHeld=true; break end end
		if anyHeld then proxyMove(State.lastMoveDir, spd)
		else proxyStop() end
	else
		if proxy then proxy.AssemblyLinearVelocity = Vector3.new(0, proxy.AssemblyLinearVelocity.Y, 0) end
	end
	if speedLbl then
		local p = ensureProxy()
		local vel = p and p.AssemblyLinearVelocity or Vector3.zero
		speedLbl.Text = "Speed: "..string.format("%.1f", Vector3.new(vel.X,0,vel.Z).Magnitude)
	end
end)

-- ===================================================================
-- CARRY AUTO DETECT — loop indépendant, throttle 0.15s
-- ===================================================================
local _carryRayParams = RaycastParams.new()
_carryRayParams.FilterType = Enum.RaycastFilterType.Exclude
local _carryLastCheck = 0
RunService.Heartbeat:Connect(function()
	local now = tick(); if now - _carryLastCheck < 0.15 then return end
	_carryLastCheck = now
	if tick() < State._carryManualUntil then return end  -- respecte le verrou manuel
	local char = LP.Character; if not char then return end
	local hrp  = char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
	_carryRayParams.FilterDescendantsInstances = {char}
	local res = workspace:Raycast(hrp.Position, Vector3.new(0,-5,0), _carryRayParams)
	if not res or not res.Instance then return end
	local plot = getPlotOf(res.Instance)
	if State.speedType ~= "carry" then
		if res.Instance.Name == "Part" and plot and not isMyPlotByName(plot.Name) then
			State.speedType = "carry"; refreshUIToggles()
			if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(true) end
			if setCarryToggle then setCarryToggle(true) end
		end
	else
		if plot and isMyPlotByName(plot.Name) then
			State.speedType = "normal"; refreshUIToggles()
			if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(false) end
			if setCarryToggle then setCarryToggle(false) end
		end
	end
end)

-- Anti-chute sous la map (technique SZG)
RunService.Stepped:Connect(function()
	local c = LP.Character
	if c then
		local r = c:FindFirstChild("HumanoidRootPart")
		if r and r.Position.Y < -10 then
			r.Position = Vector3.new(r.Position.X, -6.5, r.Position.Z)
		end
	end
end)

-- ===================================================================
-- KEYBINDS
-- ===================================================================
UIS.InputBegan:Connect(function(inp, gpe)
	if _anyKeyListening then return end
	local isGP = inp.UserInputType.Name:match("^Gamepad") ~= nil
	if inp.UserInputType == Enum.UserInputType.Keyboard then
		if gpe or UIS:GetFocusedTextBox() then return end
	elseif not isGP then return end
	local kc = inp.KeyCode
	-- Keybinds des widgets (manette/clavier)
	for _, b in ipairs(UIR.WidgetBinds) do
		if b.key ~= Enum.KeyCode.Unknown and kc == b.key then pcall(b.cb) end
	end
	if State.keyAutoLeft ~= Enum.KeyCode.Unknown and kc == State.keyAutoLeft then
		State.autoLeftEnabled = not State.autoLeftEnabled
		if UIR.setAutoLeft then UIR.setAutoLeft(State.autoLeftEnabled) end
		if MobileButtons.Buttons.autoLeft then MobileButtons.Buttons.autoLeft(State.autoLeftEnabled) end
		if State.autoLeftEnabled then startAutoLeft() else stopAutoLeft() end
		autoSaveConfig()
	end
	if State.keyAutoRight ~= Enum.KeyCode.Unknown and kc == State.keyAutoRight then
		State.autoRightEnabled = not State.autoRightEnabled
		if UIR.setAutoRight then UIR.setAutoRight(State.autoRightEnabled) end
		if MobileButtons.Buttons.autoRight then MobileButtons.Buttons.autoRight(State.autoRightEnabled) end
		if State.autoRightEnabled then startAutoRight() else stopAutoRight() end
		autoSaveConfig()
	end
	if State.keyDropBR ~= Enum.KeyCode.Unknown and kc == State.keyDropBR then
		runDropBrainrot()
		if UIR.setMenuDropBR then UIR.setMenuDropBR(true) end
		task.delay(0.5,function() if UIR.setMenuDropBR then UIR.setMenuDropBR(false) end end)
	end
	if State.keyTpDown ~= Enum.KeyCode.Unknown and kc == State.keyTpDown then
		tpToGround()
		if UIR.setMenuTpDown then UIR.setMenuTpDown(true) end
		task.delay(0.5,function() if UIR.setMenuTpDown then UIR.setMenuTpDown(false) end end)
	end
	if State.keyAutoBat ~= Enum.KeyCode.Unknown and kc == State.keyAutoBat then
		State.autoBatToggled = not State.autoBatToggled
		if State.autoBatToggled then startBatAimbot() else stopBatAimbot() end
		if UIR.setAutoBat then UIR.setAutoBat(State.autoBatToggled) end
		if MobileButtons.Buttons.autoBat then MobileButtons.Buttons.autoBat(State.autoBatToggled) end
		autoSaveConfig()
	end
	if State.keyCarrySpeed ~= Enum.KeyCode.Unknown and kc == State.keyCarrySpeed then
		toggleSpeedType()
	end
	if State.keyLaggerMode ~= Enum.KeyCode.Unknown and kc == State.keyLaggerMode then
		toggleLagger()
		if setLaggerToggle then setLaggerToggle(State.laggerActive) end
		if MobileButtons.Buttons.lagger then MobileButtons.Buttons.lagger(State.laggerActive) end
		autoSaveConfig()
	end
	if State.keyLaggerUse ~= Enum.KeyCode.Unknown and kc == State.keyLaggerUse then
		toggleLaggerUse()
		if UIR.setLaggerUse then UIR.setLaggerUse(State.laggerUseEnabled) end
		if MobileButtons.Buttons.laggerUse then MobileButtons.Buttons.laggerUse(State.laggerUseEnabled) end
		autoSaveConfig()
	end
	if State.keyInstaReset ~= Enum.KeyCode.Unknown and kc == State.keyInstaReset then
		instaReset()
		if setMenuInstaReset then setMenuInstaReset(true) end
		task.delay(0.5, function() if setMenuInstaReset then setMenuInstaReset(false) end end)
	end
	if State.keyTpUp ~= Enum.KeyCode.Unknown and kc == State.keyTpUp then
		tpUp()
		if setMenuTpUp then setMenuTpUp(true) end
		task.delay(0.5, function() if setMenuTpUp then setMenuTpUp(false) end end)
	end
end)

-- ===================================================================
-- LOAD CONFIG
-- ===================================================================
local function loadConfig()
	local hasFile = false; pcall(function() hasFile = isfile("rbxdata_cfg9x.json") end)
	if not hasFile then return end
	local ok, cfg = pcall(function() return HttpService:JSONDecode(readfile("rbxdata_cfg9x.json")) end)
	if not ok or not cfg then return end
	if cfg.normalSpeed then State.normalSpeed = cfg.normalSpeed; UIR.normalBox.Text = tostring(cfg.normalSpeed) end
	if cfg.carrySpeed then State.carrySpeed = cfg.carrySpeed; UIR.carryBox.Text = tostring(cfg.carrySpeed) end
	if cfg.laggerSpeed then State.laggerSpeed = cfg.laggerSpeed; UIR.laggerBox.Text = tostring(cfg.laggerSpeed) end
	if cfg.laggerCarrySpeed then State.laggerCarrySpeed = cfg.laggerCarrySpeed; UIR.carryLaggerBox.Text = tostring(cfg.laggerCarrySpeed) end
	if cfg.speedType then State.speedType = cfg.speedType end
	if cfg.laggerActive ~= nil then State.laggerActive = cfg.laggerActive end
	if cfg.laggerCarryActive ~= nil then State.laggerCarryActive = cfg.laggerCarryActive end
	if cfg.grabRadius then AutoSteal.Radius = cfg.grabRadius; radiusBox.Text = tostring(cfg.grabRadius); if UIR.progressRadLbl then UIR.progressRadLbl.Text = tostring(cfg.grabRadius) end end
	if cfg.grabDuration then AutoSteal.Duration = cfg.grabDuration; durationBox.Text = tostring(cfg.grabDuration) end
	if cfg.autoCarryOnGrab ~= nil then State.autoCarryOnGrab = cfg.autoCarryOnGrab; if UIR.setAutoCarry then UIR.setAutoCarry(cfg.autoCarryOnGrab) end end
	if cfg.autoStealEnabled then AutoSteal.Enabled = true; UIR.setInstaGrab(true); startAutoSteal() end
	if cfg.infJump then State.infJumpEnabled = true; if UIR.setInfJumpOld then UIR.setInfJumpOld(true) end end
	if cfg.infJumpMode then State.infJumpMode = cfg.infJumpMode end
	if cfg.autoTpDown ~= nil then State.autoTpDownEnabled = cfg.autoTpDown; if UIR.setAutoTpDown then UIR.setAutoTpDown(cfg.autoTpDown) end end
	if cfg.autoTpDownY then State.autoTpDownY = cfg.autoTpDownY end
	if cfg.laggerUseAmount then State.laggerUseAmount = cfg.laggerUseAmount; if UIR.laggerUseBox then UIR.laggerUseBox.Text = tostring(cfg.laggerUseAmount) end end
	if cfg.laggerUseEnabled then State.laggerUseEnabled = cfg.laggerUseEnabled; if UIR.setLaggerUse then UIR.setLaggerUse(cfg.laggerUseEnabled) end end
	if cfg.antiRagdoll then State.antiRagdollEnabled = true; UIR.setAntiRag(true); startAntiRagdoll() end
	if cfg.fpsBoost then State.fpsBoostEnabled = true; UIR.setFps(true); applyFPSBoost() end
	if cfg.medusaCounter then State.medusaCounterEnabled = true; UIR.setMedusaCounter(true); setupMedusaCounter(LP.Character) end
	if cfg.unwalkEnabled then UIR.setUnwalkToggle(true); task.spawn(function() task.wait(0.5); State.unwalkEnabled=false; startUnwalk() end) end
	if cfg.autoBatToggled ~= nil then State.autoBatToggled = cfg.autoBatToggled end
	if cfg.autoLeftEnabled ~= nil then State.autoLeftEnabled = cfg.autoLeftEnabled end
	if cfg.autoRightEnabled ~= nil then State.autoRightEnabled = cfg.autoRightEnabled end
	if cfg.mobileLocked ~= nil then MobileButtons.Locked = cfg.mobileLocked end
	local function loadKey(k,s)
		if cfg[k] and type(cfg[k])=="string" then
			local ok2,kc = pcall(function() return Enum.KeyCode[cfg[k]] end)
			if ok2 and kc then State[s] = kc end
		end
	end
	loadKey("keyAutoLeft","keyAutoLeft"); loadKey("keyAutoRight","keyAutoRight")
	loadKey("keyDropBR","keyDropBR"); loadKey("keyTpDown","keyTpDown")
	loadKey("keyAutoBat","keyAutoBat"); loadKey("keyCarrySpeed","keyCarrySpeed")
	loadKey("keyTpUp","keyTpUp"); loadKey("keyLaggerMode","keyLaggerMode")
	loadKey("keyInstaReset","keyInstaReset")
	loadKey("keyLaggerUse","keyLaggerUse")
	if cfg.fovValue then fovValue=cfg.fovValue end
	if cfg.fovEnabled then fovEnabled=true; enableFOV() end
	task.spawn(function()
		task.wait(0.6)
		if MobileButtons.Buttons.autoBat then MobileButtons.Buttons.autoBat(State.autoBatToggled) end
		if MobileButtons.Buttons.carrySpeed then MobileButtons.Buttons.carrySpeed(State.speedType=="carry") end
		if MobileButtons.Buttons.lagger then MobileButtons.Buttons.lagger(State.laggerActive) end
		if MobileButtons.Buttons.laggerCarry then MobileButtons.Buttons.laggerCarry(State.laggerCarryActive) end
		if MobileButtons.Buttons.autoLeft then MobileButtons.Buttons.autoLeft(State.autoLeftEnabled) end
		if MobileButtons.Buttons.autoRight then MobileButtons.Buttons.autoRight(State.autoRightEnabled) end
		if State.laggerActive or State.laggerCarryActive then startAutoSteal() end
		if State.autoLeftEnabled then startAutoLeft() end
		if State.autoRightEnabled then startAutoRight() end
	end)
	refreshUIToggles()
end

loadConfig()
-- Defaults ON : FPS Boost + Anti Lag
State.fpsBoostEnabled = true; if UIR.setFps then UIR.setFps(true) end; pcall(applyFPSBoost)
if not ANL.active then ANL.start() end; if UIR.setAntiLag then UIR.setAntiLag(true) end
-- Securite anti-detection : les 3 features les plus risquees restent OFF au demarrage
if SB.lagOn then SB.lagOn=false; SB.stopLag() end
if SB.widget then SB.widget.Visible = false end
refreshUIToggles()

-- KEY SYSTEM
task.spawn(function() pcall(function()
local KEY = "YSLEM-AX72-2AUC"
local K_FILE = "YslemHubKeyV11.txt"
local function k_load() local ok,v=pcall(readfile,K_FILE); if ok and v and #v>5 then return v:gsub("%s","") end; return nil end
local function k_save(k) pcall(writefile,K_FILE,k) end
if not k_load() then
	local kgui=Instance.new("ScreenGui"); kgui.Name=DISC.name(); kgui.ResetOnSpawn=false; kgui.DisplayOrder=200; DISC.hide(kgui)
	local kbg=Instance.new("Frame"); kbg.Size=UDim2.new(1,0,1,0); kbg.BackgroundColor3=Color3.fromRGB(0,0,0); kbg.BackgroundTransparency=0.4; kbg.BorderSizePixel=0; kbg.Parent=kgui
	local card=Instance.new("Frame"); card.Size=UDim2.new(0,320,0,290); card.Position=UDim2.new(0.5,-160,0.5,-165); card.BackgroundColor3=Color3.fromRGB(10,10,14); card.BorderSizePixel=0; card.Parent=kgui
	local cc=Instance.new("UICorner"); cc.CornerRadius=UDim.new(0,16); cc.Parent=card
	local cstr=Instance.new("UIStroke"); cstr.Color=Color3.fromRGB(225,35,40); cstr.Thickness=2; cstr.Parent=card
	local kt1=Instance.new("TextLabel"); kt1.Size=UDim2.new(1,0,0,34); kt1.Position=UDim2.new(0,0,0,16); kt1.BackgroundTransparency=1; kt1.Text="YSLEM HUB"; kt1.TextColor3=Color3.fromRGB(255,255,255); kt1.Font=Enum.Font.GothamBlack; kt1.TextSize=24; kt1.Parent=card
	local kt2=Instance.new("TextLabel"); kt2.Size=UDim2.new(1,0,0,20); kt2.Position=UDim2.new(0,0,0,52); kt2.BackgroundTransparency=1; kt2.Text="Key Required"; kt2.TextColor3=Color3.fromRGB(225,35,40); kt2.Font=Enum.Font.GothamBold; kt2.TextSize=14; kt2.Parent=card
	local kt3=Instance.new("TextLabel"); kt3.Size=UDim2.new(1,-20,0,32); kt3.Position=UDim2.new(0,10,0,76); kt3.BackgroundTransparency=1; kt3.TextWrapped=true; kt3.TextXAlignment=Enum.TextXAlignment.Left; kt3.Text="1. Join Discord  2. Copy the key in #key  3. Enter it below"; kt3.TextColor3=Color3.fromRGB(90,90,100); kt3.Font=Enum.Font.GothamBold; kt3.TextSize=12; kt3.Parent=card
	local kbox=Instance.new("TextBox"); kbox.Size=UDim2.new(1,-20,0,42); kbox.Position=UDim2.new(0,10,0,114); kbox.BackgroundColor3=Color3.fromRGB(26,14,16); kbox.BorderSizePixel=0; kbox.Text=""; kbox.PlaceholderText="YSLEM-XXXX-XXXX"; kbox.TextColor3=Color3.fromRGB(255,255,255); kbox.PlaceholderColor3=Color3.fromRGB(90,90,100); kbox.Font=Enum.Font.GothamBold; kbox.TextSize=15; kbox.ClearTextOnFocus=false; kbox.Parent=card
	local kbc=Instance.new("UICorner"); kbc.CornerRadius=UDim.new(0,8); kbc.Parent=kbox
	local kbp=Instance.new("UIPadding"); kbp.PaddingLeft=UDim.new(0,10); kbp.Parent=kbox
	local kstat=Instance.new("TextLabel"); kstat.Size=UDim2.new(1,0,0,16); kstat.Position=UDim2.new(0,0,0,162); kstat.BackgroundTransparency=1; kstat.Text=""; kstat.TextColor3=Color3.fromRGB(255,60,60); kstat.Font=Enum.Font.GothamBold; kstat.TextSize=12; kstat.Parent=card
	local kbtn=Instance.new("TextButton"); kbtn.Size=UDim2.new(1,-20,0,42); kbtn.Position=UDim2.new(0,10,0,182); kbtn.BackgroundColor3=Color3.fromRGB(225,35,40); kbtn.BorderSizePixel=0; kbtn.Text="Confirm Key"; kbtn.TextColor3=Color3.fromRGB(255,255,255); kbtn.Font=Enum.Font.GothamBlack; kbtn.TextSize=15; kbtn.Parent=card
	local kbtnc=Instance.new("UICorner"); kbtnc.CornerRadius=UDim.new(0,8); kbtnc.Parent=kbtn
	local kdisc=Instance.new("TextButton"); kdisc.Size=UDim2.new(1,-20,0,36); kdisc.Position=UDim2.new(0,10,0,238); kdisc.BackgroundColor3=Color3.fromRGB(88,101,242); kdisc.BorderSizePixel=0; kdisc.Text="Join Discord"; kdisc.TextColor3=Color3.fromRGB(255,255,255); kdisc.Font=Enum.Font.GothamBold; kdisc.TextSize=13; kdisc.Parent=card
	local kdc=Instance.new("UICorner"); kdc.CornerRadius=UDim.new(0,8); kdc.Parent=kdisc
	kdisc.Activated:Connect(function() pcall(function() setclipboard("discord.gg/mJN8wTQqEv") end); kdisc.Text="Copied!"; task.delay(2,function() kdisc.Text="Join Discord" end) end)
	local function k_unlock() kgui:Destroy() end
	local function k_try()
		local entered=kbox.Text:upper():gsub("%s","")
		if entered==KEY then k_save(entered); kstat.Text="Key valid!"; kstat.TextColor3=Color3.fromRGB(80,255,120); kbtn.Text="Unlocking..."; kbtn.BackgroundColor3=Color3.fromRGB(80,255,120); task.delay(0.8,k_unlock)
		else kstat.Text="Invalid key."; kstat.TextColor3=Color3.fromRGB(255,60,60) end
	end
	kbtn.Activated:Connect(k_try)
	kbox.FocusLost:Connect(function(enter) if enter then k_try() end end)
	task.spawn(function() while kgui and kgui.Parent do task.wait(0.5); if kbox.Text:upper():gsub("%s","")==KEY then k_try(); break end end end)
end
end) end)

print("[Yslem Hub v2] Charge avec succes")
